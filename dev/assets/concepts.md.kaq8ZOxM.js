import{_ as a,o as i,c as n,ao as e}from"./chunks/framework.t06IGxWj.js";const d=JSON.parse('{"title":"Key concepts","description":"","frontmatter":{},"headers":[],"relativePath":"concepts.md","filePath":"concepts.md","lastUpdated":null}'),t={name:"concepts.md"};function p(l,s,h,o,r,c){return i(),n("div",null,[...s[0]||(s[0]=[e(`<h1 id="Key-concepts" tabindex="-1">Key concepts <a class="header-anchor" href="#Key-concepts" aria-label="Permalink to &quot;Key concepts {#Key-concepts}&quot;">​</a></h1><p>StrictMode&#39;s guarantees enforce properties that Julia performance relies on. This page explains what those properties are and why they matter — useful background before the <a href="./guarantees">Guarantees</a> reference.</p><h2 id="Type-stability" tabindex="-1">Type stability <a class="header-anchor" href="#Type-stability" aria-label="Permalink to &quot;Type stability {#Type-stability}&quot;">​</a></h2><p>A function is <strong>type-stable</strong> when the compiler can determine the return type from the argument types alone, without running the function. With a known concrete return type, the compiler generates tight machine code and can chain optimizations across calls.</p><p><strong>Type instability</strong> produces a <code>Union</code> or <code>Any</code> return — the compiler doesn&#39;t know which type it&#39;s dealing with until the function actually runs:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> pick</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(items, i)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    items[i]          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># items = (1, 2.0, &quot;three&quot;) → return is Union{Int64,Float64,String}</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>JIT compiles pick(items, i):</span></span>
<span class="line"><span>                    │</span></span>
<span class="line"><span>      ┌─────────────┴───────────────┐</span></span>
<span class="line"><span> return type known              return type unknown</span></span>
<span class="line"><span> (always Float64)               (Union / Any)</span></span>
<span class="line"><span>      │                              │</span></span>
<span class="line"><span>  Native code:                  Generic slow path:</span></span>
<span class="line"><span>  vmulsd, vaddsd…               allocate a &quot;box&quot; on the heap</span></span>
<span class="line"><span>  ~1 ns/call                    check type at runtime</span></span>
<span class="line"><span>                                 ~100 ns/call</span></span></code></pre></div><p><code>@assert_typestable</code> and <code>@strict</code> catch the unstable path.</p><h2 id="Boxing" tabindex="-1">Boxing <a class="header-anchor" href="#Boxing" aria-label="Permalink to &quot;Boxing {#Boxing}&quot;">​</a></h2><p><strong>Boxing</strong> is what the compiler does when it can&#39;t predict a value&#39;s type: it wraps the value in a heap-allocated container — a &quot;box&quot; — with a type tag, so it can be handled generically at runtime. Unboxing it later costs a heap lookup and a type-check per value.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Unboxed (type-stable path — fast):</span></span>
<span class="line"><span>┌────────┬────────┬────────┬────────┐</span></span>
<span class="line"><span>│  1.0   │  2.0   │  3.0   │  4.0   │  ← raw 64-bit doubles, contiguous in memory</span></span>
<span class="line"><span>└────────┴────────┴────────┴────────┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Boxed (type-unknown path — slow):</span></span>
<span class="line"><span>┌────────┬────────┬────────┬────────┐</span></span>
<span class="line"><span>│  ptr   │  ptr   │  ptr   │  ptr   │  ← each element is a pointer to a heap object</span></span>
<span class="line"><span>└───┬────┴───┬────┴────────┴────────┘</span></span>
<span class="line"><span>    ↓        ↓</span></span>
<span class="line"><span>┌───────┐ ┌───────┐</span></span>
<span class="line"><span>│ tag   │ │ tag   │   ← type tag checked at runtime before every use</span></span>
<span class="line"><span>│ 1.0   │ │ 2.0   │</span></span>
<span class="line"><span>└───────┘ └───────┘</span></span></code></pre></div><p>In a tight numeric loop, the difference between boxed and unboxed is often 100× or more. The most common cause is indexing a heterogeneous tuple with a runtime index:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;three&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> t[i]    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># x is boxed — the compiler doesn&#39;t know which element type it gets</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>The fix is <code>@unroll</code>, which replaces the runtime index with compile-time literals:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@unroll</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> t[i]    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># expands to t[1], t[2], t[3] — each a known concrete type, no box</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p><code>@assert_noboxing</code> and <code>@assert_noalloc</code> catch boxing. <code>@explain</code> identifies which access is the source.</p><h2 id="Dynamic-dispatch" tabindex="-1">Dynamic dispatch <a class="header-anchor" href="#Dynamic-dispatch" aria-label="Permalink to &quot;Dynamic dispatch {#Dynamic-dispatch}&quot;">​</a></h2><p><strong>Dynamic dispatch</strong> is a function call resolved at runtime instead of compile time. It happens when the compiler doesn&#39;t know the concrete type of the receiver, so it can&#39;t select the method at compile time and must look it up during execution.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> apply</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(f, x)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    f</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x)    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># if f&#39;s concrete type is unknown, every call pays a table lookup</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>In hot loops, even one dynamic dispatch per iteration can dominate runtime. Type-unstable code usually triggers it: once a value is boxed, everything downstream dispatches dynamically. <code>@assert_noboxing</code> catches it because the JIT-level evidence is the same boxing pattern.</p><h2 id="SIMD-vectorization" tabindex="-1">SIMD vectorization <a class="header-anchor" href="#SIMD-vectorization" aria-label="Permalink to &quot;SIMD vectorization {#SIMD-vectorization}&quot;">​</a></h2><p>Modern CPUs have special <strong>vector registers</strong> that are wider than a single number. Instead of holding one <code>Float64</code>, they hold 4 or 8 at once. A single CPU instruction can then operate on all values simultaneously. This is <strong>SIMD</strong> — Single Instruction, Multiple Data.</p><p>The two common widths on modern x86 CPUs:</p><ul><li><p><strong>AVX2</strong> (most laptops and servers since ~2013): 256-bit registers → 4 doubles or 8 floats per instruction</p></li><li><p><strong>AVX-512</strong> (newer server and desktop CPUs): 512-bit registers → 8 doubles or 16 floats per instruction</p></li></ul><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Scalar loop — one value per CPU instruction:</span></span>
<span class="line"><span>  step 1:  a[0] × b[0] → c[0]</span></span>
<span class="line"><span>  step 2:  a[1] × b[1] → c[1]   ← 4 separate multiply instructions</span></span>
<span class="line"><span>  step 3:  a[2] × b[2] → c[2]</span></span>
<span class="line"><span>  step 4:  a[3] × b[3] → c[3]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>SIMD loop (AVX2) — 4 values per CPU instruction:</span></span>
<span class="line"><span>  step 1:  ┌a[0]┐   ┌b[0]┐   ┌c[0]┐</span></span>
<span class="line"><span>           │a[1]│ × │b[1]│ = │c[1]│   ← 1 vmulpd instruction, same clock cost</span></span>
<span class="line"><span>           │a[2]│   │b[2]│   │c[2]│</span></span>
<span class="line"><span>           └a[3]┘   └b[3]┘   └c[3]┘</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Result: 4× the arithmetic done in the same number of clock cycles.</span></span></code></pre></div><p><strong>Why SIMD matters independently of multi-threading.</strong> Adding threads spreads work across CPU cores — but each core still processes one value at a time without SIMD. SIMD makes <em>each core</em> faster; threading runs more cores in parallel. They multiply:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>4-core CPU with AVX2 (4 doubles/instruction):</span></span>
<span class="line"><span></span></span>
<span class="line"><span>  Without SIMD, with 4 threads:   4 cores × 1 double/instr  =  4 doubles per clock</span></span>
<span class="line"><span>  With SIMD, 1 thread:            1 core  × 4 doubles/instr =  4 doubles per clock</span></span>
<span class="line"><span>  With SIMD, 4 threads:           4 cores × 4 doubles/instr = 16 doubles per clock  ← full throughput</span></span></code></pre></div><p>A program that uses threads but not SIMD, running on a 4-core AVX2 machine, is leaving 75% of its compute unused. Threads and SIMD are complementary — you need both to saturate the hardware.</p><p>Julia&#39;s compiler generates SIMD instructions automatically — but only when the loop structure allows it: no data-dependent branches, sequential memory access, no unresolved function calls, and type-stable element types. A loop that looks vectorized often isn&#39;t.</p><p><code>@assert_vectorized</code> checks that the compiled output actually contains vector instructions. <code>kernel_report</code> explains <em>why</em> a vectorized kernel might still be slow — arithmetic intensity, alignment, register pressure.</p><h2 id="If-you&#39;re-coming-from-C,-C,-or-Java" tabindex="-1">If you&#39;re coming from C, C++, or Java <a class="header-anchor" href="#If-you&#39;re-coming-from-C,-C,-or-Java" aria-label="Permalink to &quot;If you&#39;re coming from C, C++, or Java {#If-you&#39;re-coming-from-C,-C,-or-Java}&quot;">​</a></h2><p>You already know the underlying model. The Julia concepts map directly to things you&#39;ve worked with — the key difference is that Julia infers types rather than requiring declarations.</p><p><strong>Type stability ↔ concrete return types.</strong></p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// C++: void* return forces a runtime cast — same overhead as Julia&#39;s Union return</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void*</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> pick_unstable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> i</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">       // caller must cast; no inlining opportunity</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">double</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> pick_stable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">double</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> x</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // concrete return → inlined, no overhead</span></span></code></pre></div><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Java: Object return means a cast on every use</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Object </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pick</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">Object</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] arr, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i) { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> arr[i]; }   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// generic, slow</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">double</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> dot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">double</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] a, </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">double</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[] b) { ... }            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// concrete, fast</span></span></code></pre></div><p>In Julia, no declaration is needed — the JIT infers the return type. If it can&#39;t (<code>Union{Float64,Int64}</code> for example), you silently get the <code>void*</code>/<code>Object</code> slow path. <code>@assert_typestable</code> catches that before you hit the profiler.</p><p><strong>Boxing ↔ Java autoboxing.</strong></p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Java: autoboxing int → Integer allocates on the heap in a hot loop</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Integer sum </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> v </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> values) sum </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> v;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// each += boxes and unboxes ← slow</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sum </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> v </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> values) sum </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> v;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// no boxing ← fast, same as Julia&#39;s unboxed path</span></span></code></pre></div><p>Julia boxes for the same reason Java does: when the JIT can&#39;t track the concrete type, it falls back to a heap-allocated wrapper. <code>@assert_noboxing</code> catches it the same way a JVM profiler flags <code>Integer</code> allocations in hot code.</p><p><strong>Dynamic dispatch ↔ virtual calls without devirtualization.</strong></p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// C++: virtual dispatch is a vtable lookup per call; devirtualization needs a concrete type</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Base</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">virtual</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> double</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> f</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; };</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> hot_loop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Base</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> b</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { b-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">f</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); }</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">     // table lookup every call — can&#39;t inline</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">void</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> hot_loop</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">Derived</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;"> d</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) { d-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">f</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(); }</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // concrete type → devirtualized, inlined</span></span></code></pre></div><p>Julia dispatches the same way. With concrete types the JIT devirtualizes and inlines; with abstract or Union types it falls back to a runtime lookup. <code>@assert_noboxing</code> flags the cases where devirtualization didn&#39;t happen.</p><p><strong>SIMD ↔ <code>-O3 -march=native</code> / intrinsics.</strong></p><div class="language-cpp vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">cpp</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// C++: auto-vectorizes given no aliasing, no branches, concrete element type</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">; i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> n; i</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">++</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) c[i] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a[i] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b[i];</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // → vmulpd with -O3 -march=native</span></span></code></pre></div><p>Julia&#39;s autovectorizer has the same requirements as GCC/Clang. <code>@assert_vectorized</code> checks the compiled IR the same way you&#39;d scan <code>objdump -d</code> for <code>ymm</code>/<code>zmm</code> registers. <code>kernel_report</code> gives the arithmetic intensity and alignment signals you&#39;d get from VTune or <code>perf</code>.</p><p>The surprise for C++/Java developers: Julia achieves the same performance without declarations, but the trap is when inference silently fails and nothing tells you. StrictMode is that signal.</p><h2 id="If-you&#39;re-coming-from-Python-or-MATLAB" tabindex="-1">If you&#39;re coming from Python or MATLAB <a class="header-anchor" href="#If-you&#39;re-coming-from-Python-or-MATLAB" aria-label="Permalink to &quot;If you&#39;re coming from Python or MATLAB {#If-you&#39;re-coming-from-Python-or-MATLAB}&quot;">​</a></h2><p>The key insight: Julia looks like Python or MATLAB, but compiles to native machine code — <em>if</em> it can figure out the types. When it can&#39;t, it silently falls back to something as slow as a Python loop.</p><p><strong>Why Julia loops can be fast where Python loops can&#39;t.</strong></p><p>In Python, every value is wrapped in a Python object with a type tag. When you write a loop, the interpreter processes one object at a time, checking types at every step:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Python loop over [1.0, 2.0, 3.0, 4.0]:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>iteration 1: unwrap PyObject → check it&#39;s a float → multiply → wrap result in new PyObject</span></span>
<span class="line"><span>iteration 2: unwrap PyObject → check it&#39;s a float → multiply → wrap result in new PyObject</span></span>
<span class="line"><span>...</span></span>
<span class="line"><span>overhead on every single step</span></span></code></pre></div><p>NumPy avoids this by storing raw numbers without wrappers, then dispatching to <strong>BLAS</strong> (Basic Linear Algebra Subprograms) — external C and Fortran libraries that were compiled ahead of time, ship as static <code>.so</code>/<code>.dll</code> files, and never change at runtime. They know exactly what types they&#39;ll receive (<code>double*</code> arrays), so they can use CPU vector instructions directly. The speed comes from those pre-compiled routines, not from Python. The limitation: BLAS handles a fixed set of operations (dot products, matrix multiply, etc.) — you can&#39;t add your own.</p><p>Julia&#39;s JIT generates that same native code for <em>your own loops</em> on the fly — no pre-compiled library required — but only when the types are stable:</p><div class="language-python vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">python</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Python loop: slow — interprets one object at a time</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">def</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> dot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a[i] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b[i] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> range</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">len</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a)))</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># NumPy: fast — dispatches to BLAS (static pre-compiled C/Fortran), operates on raw doubles</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> numpy </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> np</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">result </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> np.dot(a, b)</span></span></code></pre></div><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Julia: fast when types are stable — JIT generates native code directly</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> dot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{Float64}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, b</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{Float64}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    s </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.0</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> eachindex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        s </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> a[i] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b[i]   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># compiles to vmulsd/vaddsd — same as the C inside NumPy</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    end</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    s</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p><strong>Boxing — Python objects vs contiguous arrays.</strong></p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Python list [1.0, 2.0, 3.0]:          Julia Vector{Float64} / NumPy float64:</span></span>
<span class="line"><span></span></span>
<span class="line"><span> ptr    ptr    ptr                      ┌──────┬──────┬──────┐</span></span>
<span class="line"><span>  │      │      │                       │ 1.0  │ 2.0  │ 3.0  │</span></span>
<span class="line"><span>  ↓      ↓      ↓                       └──────┴──────┴──────┘</span></span>
<span class="line"><span>┌─────┐┌─────┐┌─────┐                  raw 64-bit doubles, no wrappers</span></span>
<span class="line"><span>│type ││type ││type │</span></span>
<span class="line"><span>│ 1.0 ││ 2.0 ││ 3.0 │  ← three separate heap objects, type-checked on every access</span></span>
<span class="line"><span>└─────┘└─────┘└─────┘</span></span></code></pre></div><p>When Julia silently boxes values in a loop, it produces the Python-list layout — even though the source code looks nothing like Python. <code>@assert_noboxing</code> catches it.</p><p><strong>SIMD — what makes numeric code fast.</strong></p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Without SIMD (what a Python loop does — one number at a time):</span></span>
<span class="line"><span>  multiply(1.0, 4.0) → 4.0</span></span>
<span class="line"><span>  multiply(2.0, 5.0) → 10.0    ← one multiplication per CPU instruction</span></span>
<span class="line"><span>  multiply(3.0, 6.0) → 18.0</span></span>
<span class="line"><span></span></span>
<span class="line"><span>With SIMD (what Julia, NumPy, and MATLAB&#39;s matrix ops do):</span></span>
<span class="line"><span>  multiply([1.0, 2.0, 3.0, 4.0], [4.0, 5.0, 6.0, 7.0])</span></span>
<span class="line"><span>      → [4.0, 10.0, 18.0, 28.0]                          ← four multiplications, one instruction</span></span></code></pre></div><div class="language-matlab vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">matlab</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">% MATLAB: fast because A*B dispatches to BLAS/LAPACK — external libraries</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">% compiled ahead of time in C and Fortran, shipped as static binaries.</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">% They know the types (double matrices), so they use vector instructions directly.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">C = A </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> B;        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">% → BLAS dgemm — highly optimized, static, processes 8+ values per instruction</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:n       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">% → scalar loop — MATLAB&#39;s own JIT may vectorize, but may not</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    c(i) = a(i) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b(i);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>BLAS and LAPACK (Linear Algebra PACKage) are the same pre-compiled static libraries that power NumPy, MATLAB, R, and most scientific computing tools. They are incredibly fast, but they only cover a fixed set of dense linear algebra operations.</p><p>Julia&#39;s JIT generates equivalent native code for <em>your own loops</em> — not just matrix multiply — but only when the loop is type-stable and the memory access is sequential. <code>@assert_vectorized</code> confirms the vector instructions are actually there.</p><p>The surprise: a Julia loop can run as fast as MATLAB&#39;s <code>A*B</code> — or 100× slower — depending entirely on whether the JIT could infer the types. The source code looks identical either way. StrictMode is the check that tells you which path you&#39;re actually on.</p><h2 id="How-they-interact" tabindex="-1">How they interact <a class="header-anchor" href="#How-they-interact" aria-label="Permalink to &quot;How they interact {#How-they-interact}&quot;">​</a></h2><p>These four issues cascade. A single root cause can trigger all of them:</p><ol><li><p>A heterogeneous tuple gets indexed at runtime → <strong>type instability</strong></p></li><li><p>The compiler can&#39;t track the type → the value is <strong>boxed</strong></p></li><li><p>Downstream operations can&#39;t select a method statically → <strong>dynamic dispatch</strong></p></li><li><p>The loop carries an opaque value → <strong>no SIMD</strong></p></li></ol><p>Fixing the root cause (often the type instability) eliminates all four. <code>@explain</code> shows which layer is the actual source, and <code>@strict</code> / <code>@kernel</code> confirm all four are resolved.</p>`,68)])])}const g=a(t,[["render",p]]);export{d as __pageData,g as default};
