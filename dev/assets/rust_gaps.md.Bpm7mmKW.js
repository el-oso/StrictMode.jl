import{_ as s,o as i,c as t,ao as a}from"./chunks/framework.Cb-Je6QH.js";const k=JSON.parse('{"title":"Closing the gaps with Rust","description":"","frontmatter":{},"headers":[],"relativePath":"rust_gaps.md","filePath":"rust_gaps.md","lastUpdated":null}'),n={name:"rust_gaps.md"};function o(r,e,l,h,d,c){return i(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="Closing-the-gaps-with-Rust" tabindex="-1">Closing the gaps with Rust <a class="header-anchor" href="#Closing-the-gaps-with-Rust" aria-label="Permalink to &quot;Closing the gaps with Rust {#Closing-the-gaps-with-Rust}&quot;">​</a></h1><p>Rust has a quiet superpower: idiomatic code is reliably fast, because the compiler (or the type system) complains in exactly the places where Julia would have stayed silent. The goal with StrictMode was to make Julia feel that predictable to optimize. Dogfooding it against a SIMD FFT turned up three specific gaps, and this page walks through each one and the feature that closes it.</p><h2 id="1.-The-time-tax-closed" tabindex="-1">1. The time tax (closed) <a class="header-anchor" href="#1.-The-time-tax-closed" aria-label="Permalink to &quot;1. The time tax (closed) {#1.-The-time-tax-closed}&quot;">​</a></h2><p>Rust&#39;s guarantees are free. StrictMode&#39;s used to cost a full AllocCheck pass for every method, enough that a whole-package sweep once blew through a ten-minute timeout. Not any more:</p><ul><li><p><code>:fast</code> mode is a value-free, inference-only triage of every property at once (allocation, boxing, type-stability), built on <code>code_typed</code> IR and <code>Base.infer_effects</code>. It needs no AllocCheck or JET backend and runs about 10× cheaper per method than <code>:full</code>, which is still there as the rigorous AllocCheck proof for CI.</p></li><li><p>An incremental cache keys findings on <code>(method, world, signature, mode)</code>, so re-running <code>audit</code> after a one-method edit only re-analyzes that one method. It comes back almost instantly.</p></li><li><p><code>:fast</code> sweeps run across threads.</p></li></ul><p>A whole-module re-check on every edit is now affordable. See <a href="./getting_started">Getting Started</a>.</p><h2 id="2.-Opt-in,-not-pervasive-closed" tabindex="-1">2. Opt-in, not pervasive (closed) <a class="header-anchor" href="#2.-Opt-in,-not-pervasive-closed" aria-label="Permalink to &quot;2. Opt-in, not pervasive (closed) {#2.-Opt-in,-not-pervasive-closed}&quot;">​</a></h2><p>Rust disciplines every function by default, and you opt out of safety rather than into it. StrictMode now works the same way:</p><ul><li><p><code>@strict module … end</code> makes every function in the module checked by default, from one declaration. No per-function annotations.</p></li><li><p><code>@strict_exempt</code> lets the occasional cold or by-design-allocating helper opt out at its definition.</p></li><li><p>The whole-module load check runs on the cheap <code>:fast</code> path, and <code>audit</code> only reports declared-hot violations, so it stays quiet by default. See <a href="./automating">Automating checks</a>.</p></li></ul><h2 id="3.-Scheduling-and-vectorization-surfaced,-not-controlled" tabindex="-1">3. Scheduling and vectorization (surfaced, not controlled) <a class="header-anchor" href="#3.-Scheduling-and-vectorization-surfaced,-not-controlled" aria-label="Permalink to &quot;3. Scheduling and vectorization (surfaced, not controlled) {#3.-Scheduling-and-vectorization-surfaced,-not-controlled}&quot;">​</a></h2><p>The gap that&#39;s left is instruction scheduling and vectorization, the layer that lives below your code. StrictMode doesn&#39;t try to <em>control</em> instruction scheduling — that&#39;s a deliberate non-goal, not a missing feature. (How high that ceiling actually is turned out to be a pleasant surprise — see the honest summary below, where pure <code>SIMD.jl</code> beat a Rust library&#39;s hand-written assembly.) What it can do is make that layer visible and reachable:</p><ul><li><p><code>@assert_vectorized f(args...)</code> asks, best-effort, whether the loop actually emitted SIMD vector ops (<code>&lt;N x …&gt;</code> in the LLVM IR), and fails loudly if it didn&#39;t.</p></li><li><p><code>@assert_effects f(args...) (:nothrow, :effect_free, …)</code> verifies the compiler&#39;s inferred effects (<code>Base.infer_effects</code>). It&#39;s the verify side of <code>Base.@assume_effects</code>.</p></li><li><p><a href="/StrictMode.jl/dev/api#StrictMode.descend"><code>descend</code></a><code>(f, types)</code> drops you into <a href="https://github.com/JuliaDebug/Cthulhu.jl" target="_blank" rel="noreferrer">Cthulhu</a> to see inlining, effects, type-stability, and the LLVM/native code (an optional weak dependency).</p></li></ul><div class="tip custom-block"><p class="custom-block-title">Validated on real SIMD kernels</p><p><code>@assert_vectorized</code> works on the explicit-vector pattern that hand-tuned numeric code actually uses, <code>SIMD.jl</code>&#39;s <code>Vec</code> <code>vload</code>/<code>vstore</code> over a preallocated buffer. Those <code>&lt;N x double&gt;</code> ops are emitted regardless of the build&#39;s CPU target, unlike <code>@simd</code> auto-vectorization, which is target-gated:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> SIMD</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> vscale!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dst</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{Float64}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, src</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{Float64}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    @inbounds</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(src)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        vstore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">vload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Vec{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,Float64}, src, i) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, dst, i)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    end</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> dst</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_vectorized</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> vscale!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dst, src)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># passes — the loop emitted vector ops</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_noalloc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    vscale!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dst, src)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># …and stayed on the fast path</span></span></code></pre></div><p>Dogfooded against PureFFT.jl: <code>@assert_vectorized</code> confirms the leaf AVX compute kernels (<code>_base_butterflies_avx!</code>, <code>_fft128_avx!</code>) emit vector ops, and correctly reports that the top-level dispatcher (<code>apply_unnormalized!</code>) does not vectorize directly, since it just routes to those kernels. That&#39;s an honest, informative signal rather than a false positive, and <code>@assert_noalloc</code> confirms the kernels stay allocation-free.</p></div><h3 id="The-ceiling,-and-the-escape-hatch" tabindex="-1">The ceiling, and the escape hatch <a class="header-anchor" href="#The-ceiling,-and-the-escape-hatch" aria-label="Permalink to &quot;The ceiling, and the escape hatch {#The-ceiling,-and-the-escape-hatch}&quot;">​</a></h3><p>When the compiler&#39;s scheduling decisions are the bottleneck, the levers, in increasing order of control, are:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Lever</th><th style="text-align:right;">What it influences</th></tr></thead><tbody><tr><td style="text-align:right;"><code>@inbounds</code>, <code>@simd ivdep</code></td><td style="text-align:right;">removes bounds checks / asserts no loop-carried deps → enables vectorization</td></tr><tr><td style="text-align:right;"><code>Base.@assume_effects</code></td><td style="text-align:right;">declares effects (<code>:nothrow</code>, <code>:consistent</code>, …) the compiler can build on</td></tr><tr><td style="text-align:right;"><code>Base.llvmcall</code></td><td style="text-align:right;">hand-written LLVM IR, the final escape hatch for the hottest kernels</td></tr></tbody></table><p>StrictMode doesn&#39;t wrap these, since they&#39;re already in Base. What it adds is a way to keep your escape-hatch kernels verifiable: assert that your <code>llvmcall</code> kernel is still allocation-free, and <code>descend</code> into it to confirm the codegen:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># hand-written IR for a hot kernel…</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">addll</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Int64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Int64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Base</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">llvmcall</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;%z = add i64 %0, %1</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">ret i64 %z&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, Int64, Tuple{Int64,Int64}, x, y)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_noalloc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> addll</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># …still on the fast path</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">descend</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(addll, (Int64, Int64))  </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># …and see exactly what it compiled to</span></span></code></pre></div><p>The honest summary: with StrictMode, idiomatic Julia is about as predictable to optimize as Rust — and the ceiling is higher than this page first assumed. A later QR-factorization port found that pure <code>SIMD.jl</code>, lowered by LLVM with <strong>no</strong> hand-written IR, matched and then <strong>beat</strong> a state-of-the-art Rust library&#39;s hand-written <strong>assembly</strong> gemm (73 vs 70 GFLOP/s, single-thread) once the kernel&#39;s memory orchestration matched. So raw instruction scheduling is not reliably rustc&#39;s private domain: LLVM-from-idiomatic-<code>SIMD.jl</code> reached the same ceiling, and the residual gap was <strong>algorithmic</strong> — how the surrounding code blocks and streams memory — which is human roofline reasoning, not a language or codegen limit. The <code>Base.llvmcall</code> escape hatch stays available and verifiable, but in that case it proved unnecessary (an inline-asm version was <em>slower</em> than the SIMD.jl one).</p><div class="tip custom-block"><p class="custom-block-title">Scope of that claim</p><p>This is parity of <em>ceiling</em> on a specific kernel and machine — evidence that the language/codegen is not the bottleneck — <strong>not</strong> a claim that Julia is faster than Rust in general. The defensible statement is: where the Rust library was ahead, the cause was an algorithmic orchestration choice (portable to either language), not Rust, LLVM, or the algorithm; pure Julia reproduced it and pulled ahead. Most kernels matched the reference <em>bit-for-bit</em>; one SIMD reduction (<code>inner_prod</code>) did not, because reduction order is LLVM-codegen-defined — see <a href="./guarantees">the promise scope</a> on why bit-reproducibility is not a StrictMode guarantee.</p></div><h3 id="Necessary,-but-not-sufficient:-kernel_report" tabindex="-1">Necessary, but not sufficient: <code>kernel_report</code> <a class="header-anchor" href="#Necessary,-but-not-sufficient:-kernel_report" aria-label="Permalink to &quot;Necessary, but not sufficient: \`kernel_report\` {#Necessary,-but-not-sufficient:-kernel_report}&quot;">​</a></h3><p>Dogfooding a faer-style blocked Cholesky made the ceiling concrete. We had three trailing-update (<code>syrk</code>) kernels: a naive <code>Vec</code> loop, a register-blocked one, and a <code>@turbo</code> one. All three pass <code>@assert_vectorized</code>, <code>@assert_noalloc</code>, and <code>@assert_typestable</code> identically, and yet they differ by about 2× (and roughly 6× against the base kernel). The correctness-style guarantees turn out to be necessary but not sufficient: they confirm <code>&lt;N x double&gt;</code> is being emitted, but they say nothing about register blocking, cache blocking, or the FLOP:byte ratio, which is exactly what separates a toy SIMD loop from a real microkernel.</p><p><a href="/StrictMode.jl/dev/api#StrictMode.kernel_report"><code>kernel_report</code></a> is the answer. It&#39;s a non-failing performance diagnostic that reads the arithmetic intensity (FP vector ops against memory vector ops) straight from the LLVM IR, so a green-but-slow kernel can be seen to be memory-bound. That points you at the lever instead of leaving you to discover it by benchmarking:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;" class="repl-prompt repl-prompt-julia">julia</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;" class="repl-prompt repl-prompt-julia">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> kernel_report</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(syrk_naive!, (Matrix{Float64},))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KernelReport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> syrk_naive!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Matrix{Float64})</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  vectorized — </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`&lt;8 x&gt;\`</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  FP vector ops </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> memory vector ops </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> :</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 12</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  →</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> arithmetic intensity </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.67</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  →</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> memory</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bound</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> streams more than it computes. Reuse loaded vectors across more FMAs</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (register blocking) and tile the reduction dimension (cache blocking)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span></span></code></pre></div><p>It&#39;s heuristic and advisory, not a profiler or a roofline, but it surfaces the kind of problem (memory- versus compute-bound) that the pass/fail guarantees structurally can&#39;t. Think of it as the diagnostic layer sitting just beneath the guarantees.</p><p>Two additional signals complement intensity (F13):</p><ul><li><p><strong>Alignment</strong> (<code>unaligned_mem_ops</code>): vector loads/stores whose recorded <code>align</code> annotation is less than the vector width in bytes — a proxy for buffer-alignment issues that can stall wide SIMD. A triangular loop that starts at the diagonal instead of the column base, for example, will show up here.</p></li><li><p><strong>Masking</strong> (<code>masked_mem_ops</code>): <code>@llvm.masked.*</code> intrinsics — a proxy for variable-length inner-loop trip counts (remainder tiling). Fixed-width tiles eliminate these.</p></li></ul><p>For cache-residency context (F14/F15), pass <code>working_set_bytes</code>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;" class="repl-prompt repl-prompt-julia">julia</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;" class="repl-prompt repl-prompt-julia">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> kernel_report</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(syrk_naive!, (Matrix{Float64},); working_set_bytes </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">64</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → &quot;L1-resident. Low intensity is acceptable at this size; memory-bound advice applies as n grows.&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;" class="repl-prompt repl-prompt-julia">julia</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;" class="repl-prompt repl-prompt-julia">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> kernel_report</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(syrk_tiled!, (Matrix{Float64},); working_set_bytes </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">512</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">512</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → &quot;Good register intensity, but working set spills L3 — BLIS-style packing needed</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#    for the cache-locality leg (beyond per-kernel IR analysis).&quot;</span></span></code></pre></div><p>The second case (F15) is the limit of what per-kernel IR inspection can guide: once the working set exceeds L2 and register blocking is already good, closing the gap requires BLIS-style packing of the panel operand — a memory-traffic decision above the single-kernel view. <code>kernel_report</code> names the ceiling; the human has to cross it.</p><p>The default cache thresholds (L1=32 KiB, L2=512 KiB, L3=16 MiB) match a typical desktop/server. Tune them with <code>StrictMode._CACHE_BYTES[] = (l1 = …, l2 = …, l3 = …)</code>.</p><p>The ceiling also shows up in algorithmic choices that sit above the per-kernel view. In a QR factorization port, two versions of the same panel-gemm accumulation kernel were fully green on every guarantee — vectorized, allocation-free, type-stable — yet differed by ~25% (58 vs 73 GFLOP/s). The difference was an orchestration choice: one version read the large operand in place; the other packed it first to improve cache reuse. <code>@assert_vectorized</code> and <code>kernel_report</code> reported identical results for both. The guarantees confirmed that each kernel was healthy at the LLVM-IR level, but the performance gap lived above the per-kernel view, in how the surrounding code accessed memory. (That faster, read-in-place version is the one that went on to beat the reference Rust library&#39;s hand-written assembly — see the honest summary above. The point here is that StrictMode&#39;s guarantees correctly certified <em>both</em> kernels as healthy and, rightly, stayed silent about the orchestration choice that actually decided the race: that decision is the human&#39;s, and it sits above what per-kernel IR inspection can see.)</p><p>The division of labor is worth stating plainly. The asserts defend the <strong>floor</strong>: the necessary properties (vectorized, allocation-free, type-stable) whose loss costs you 2–100× silently. Staying above that floor is necessary but not sufficient for peak performance — the <strong>sufficiency</strong> layer, cache and register blocking and microkernel scheduling, still takes human roofline reasoning. <code>kernel_report</code> is the first locality/intensity diagnostic aimed at that layer, and it&#39;s meant to <em>complement</em> the guardrails, not replace them: keep asserting the invariants, and reach for the diagnostic when something is green but still slow.</p><h3 id="Three-more-dimensions:-branches,-serial-dependencies,-and-data-distribution-F24/F28/F26" tabindex="-1">Three more dimensions: branches, serial dependencies, and data distribution (F24/F28/F26) <a class="header-anchor" href="#Three-more-dimensions:-branches,-serial-dependencies,-and-data-distribution-F24/F28/F26" aria-label="Permalink to &quot;Three more dimensions: branches, serial dependencies, and data distribution (F24/F28/F26) {#Three-more-dimensions:-branches,-serial-dependencies,-and-data-distribution-F24/F28/F26}&quot;">​</a></h3><p>The same &quot;guarantees are necessary-but-not-sufficient&quot; gap recurs in three additional forms, all confirmed by the GP-crate porting campaign:</p><p><strong>Branch misprediction (F24)</strong>: a data-dependent <code>if x &lt; 0</code> (50/50 signs) in a numeric kernel ran ~4× slower than the branchless equivalent, while <code>@assert_vectorized</code>, <code>@assert_noalloc</code>, and <code>@assert_typestable</code> all stayed green. The fix — <code>ifelse</code>/cmov, unconditional computation, <code>Base.ifelse</code> to force predication — flipped a 0.92× &quot;loss&quot; into a 1.5× beat of the Rust crate. <code>kernel_report</code> now counts conditional branches in vectorized functions as a mispredict signal (advisory: <code>branch_count</code>), but can&#39;t predict the data distribution.</p><p><strong>Serial loop-carried dependency (F28)</strong>: a <code>÷10</code> chain in itoa, a <code>÷100</code> trim loop in <code>Base.Ryu.writeshortest</code> — each iteration&#39;s inputs derived from the previous via a ~10–20 cycle <code>div</code> op — is latency-bound regardless of branch prediction or vectorization. It allocates nothing, type-stable, isn&#39;t a vectorization candidate, passes <code>:no_scalar_loops</code>. The fix is to break the chain (process two digits/values per step via <code>÷100</code>, or restructure to independent chunks). <code>kernel_report</code> now counts <code>div</code>/<code>rem</code>/<code>sqrt</code> in functions with loop-carried integers as a serialization signal (<code>serial_dep_count</code>).</p><p><strong>Value-distribution dependence (F26)</strong>: <code>Base.Ryu.writeshortest</code> measured 0.76× on <code>rand()</code> (full mantissa) but 2.05× on integer-valued floats — <em>same code path</em>, 2.7× swing. No static analysis or single benchmark captures data-domain variation. Require measuring across representative value classes and reporting the spread (see cookbook).</p><p>These three join register tiling (F13), cache-locality (F15), and algorithmic orchestration (F17) as the known &quot;above-the-guarantee&quot; performance levers that per-kernel IR analysis cannot close.</p><h3 id="Aliasing-and-effects:-what-Julia-doesn&#39;t-tell-LLVM-Issues-2-and-3" tabindex="-1">Aliasing and effects: what Julia doesn&#39;t tell LLVM (Issues 2 and 3) <a class="header-anchor" href="#Aliasing-and-effects:-what-Julia-doesn&#39;t-tell-LLVM-Issues-2-and-3" aria-label="Permalink to &quot;Aliasing and effects: what Julia doesn&#39;t tell LLVM (Issues 2 and 3) {#Aliasing-and-effects:-what-Julia-doesn&#39;t-tell-LLVM-Issues-2-and-3}&quot;">​</a></h3><p>Two structural information gaps widen the Julia–Rust codegen margin beyond what any per-kernel guarantee can close:</p><p><strong>Aliasing (Issue 2)</strong>: Rust&#39;s <code>&amp;mut T</code> tells LLVM that the reference is exclusively owned for the duration of the call — LLVM receives <code>noalias</code> on every <code>&amp;mut</code> parameter for free. In Julia, <code>Array</code> arguments are conservatively assumed potentially aliasing unless the user explicitly asserts otherwise. This lets LLVM reorder and vectorize across iterations in Rust but forces conservative dependency tracking in Julia.</p><p>The workaround:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Without: LLVM may be conservative about alias-dependent reordering</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@inbounds</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> eachindex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b); a[i] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b[i]; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># With: explicit no-alias / no-loop-carried-dep assertion</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@inbounds</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> @simd</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ivdep </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> eachindex</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b); a[i] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> b[i]; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p><code>kernel_report</code> now surfaces <code>noalias_missing_count</code> — the number of pointer parameters in the function&#39;s LLVM IR that lack the <code>noalias</code> attribute — as an advisory signal on vectorized functions. This is filed upstream as a Julia compiler improvement request (automatic <code>noalias</code> emission where the type system can prove exclusive ownership).</p><p><strong>Effect inference (Issue 3)</strong>: Rust&#39;s type system lets rustc infer that a function is effect-free (no side effects, no heap allocation, terminates) from its type signature and body. In Julia, the compiler needs manual <code>Base.@assume_effects</code> to unlock the same optimizations (constant folding, inlining at constant arguments). <code>@assert_effects</code> is the StrictMode verify side — it checks that <code>Base.infer_effects</code> agrees with a declared guarantee. Extending Julia&#39;s automatic effects inference to pure <code>@inbounds</code> SIMD loops is an open upstream improvement.</p><h2 id="Bonus:-trim-safety-the-static-binary-story" tabindex="-1">Bonus: trim-safety (the static-binary story) <a class="header-anchor" href="#Bonus:-trim-safety-the-static-binary-story" aria-label="Permalink to &quot;Bonus: trim-safety (the static-binary story) {#Bonus:-trim-safety-the-static-binary-story}&quot;">​</a></h2><p>Rust&#39;s other predictability win is ahead-of-time compilation to a small static binary. Julia&#39;s answer to that is <code>juliac --trim=safe</code>, which rejects dynamic dispatch and runtime reflection. StrictMode surfaces it as one more guarantee, powered by <a href="https://github.com/el-oso/TypeContracts.jl" target="_blank" rel="noreferrer">TypeContracts</a> (already a dependency, so no backend needed):</p><ul><li><p>The proactive side: <code>@assert_trim_safe f(args...)</code>, or the <code>:trimsafe</code> guarantee in <code>check</code> and <code>audit</code>, scans the typed IR for the patterns the trimmer rejects before you sit through the slow build:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">audit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(MyPkg; sweep </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, guarantees </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:typestable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:noalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:trimsafe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span></code></pre></div></li><li><p>The reactive side: when a real <code>juliac --trim</code> run fails, <a href="/StrictMode.jl/dev/api#StrictMode.explain_trim"><code>explain_trim</code></a> turns its cryptic verifier dump into a source-mapped explanation with per-site hints.</p></li></ul><p>(<code>@assert_trim_safe</code> and <code>:trimsafe</code> are advisory, since juliac&#39;s whole-program verifier is the authority, so like <code>@assert_vectorized</code> they&#39;re opt-in and not part of <code>@strict</code>.)</p>`,50)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
