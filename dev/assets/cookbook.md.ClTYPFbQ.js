import{_ as e,o as i,c as a,ao as t}from"./chunks/framework.t06IGxWj.js";const k=JSON.parse('{"title":"StrictMode cookbook: trap → macro","description":"","frontmatter":{},"headers":[],"relativePath":"cookbook.md","filePath":"cookbook.md","lastUpdated":null}'),n={name:"cookbook.md"};function l(o,s,r,h,d,p){return i(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="StrictMode-cookbook:-trap-macro" tabindex="-1">StrictMode cookbook: trap → macro <a class="header-anchor" href="#StrictMode-cookbook:-trap-macro" aria-label="Permalink to &quot;StrictMode cookbook: trap → macro {#StrictMode-cookbook:-trap-macro}&quot;">​</a></h1><p>A quick lookup table from the usual Julia performance traps to the StrictMode guarantee that catches each one. Turn the checks on first with <code>StrictMode.enable_checks!()</code>; in production they compile away to nothing.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Performance trap</th><th style="text-align:right;">Symptom</th><th style="text-align:right;">Catch it with</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>Runtime tuple indexing</strong> (<code>t[i]</code>, <code>t</code> heterogeneous, <code>i</code> a runtime value)</td><td style="text-align:right;"><code>Union</code> return type, silent boxing</td><td style="text-align:right;"><code>@assert_noboxing</code> / <code>@unroll</code> to fix</td></tr><tr><td style="text-align:right;"><strong>Type-unstable return</strong> (a branch returns <code>Int</code>, another <code>Float64</code>)</td><td style="text-align:right;"><code>Union{...}</code>/<code>Any</code> return; downstream boxing</td><td style="text-align:right;"><code>@assert_typestable</code></td></tr><tr><td style="text-align:right;"><strong>Captured-variable boxing</strong> (a closure mutates an outer local)</td><td style="text-align:right;"><code>Core.Box</code>, allocations, lost inference</td><td style="text-align:right;"><code>@assert_noboxing</code></td></tr><tr><td style="text-align:right;"><strong>Untyped accumulator</strong> (<code>acc = []</code> / <code>acc = 0</code> later holding mixed types)</td><td style="text-align:right;">per-iteration allocation, dispatch</td><td style="text-align:right;"><code>@assert_noalloc</code> / <code>@strict</code></td></tr><tr><td style="text-align:right;"><strong>Allocating hot loop</strong> (<code>push!</code> into a fresh <code>Vector</code>, <code>collect</code>, slices)</td><td style="text-align:right;">heap traffic, GC pressure in inner loops</td><td style="text-align:right;"><code>@assert_noalloc</code></td></tr><tr><td style="text-align:right;"><strong>Boxing, but buffers are fine</strong> (must not box, may allocate scratch space)</td><td style="text-align:right;">runtime dispatch / <code>jl_get_nth_field_checked</code> only</td><td style="text-align:right;"><code>@assert_noboxing</code> (allows typed allocations)</td></tr><tr><td style="text-align:right;"><strong>Accidental dynamic dispatch</strong> (abstract field types, <code>Any</code> args)</td><td style="text-align:right;">runtime dispatch shows as allocation</td><td style="text-align:right;"><code>@assert_noboxing</code> / <code>@assert_noalloc</code></td></tr><tr><td style="text-align:right;"><strong>A call that should inline but doesn&#39;t</strong> (cost-model misfire, <code>@noinline</code>)</td><td style="text-align:right;">call overhead, lost cross-call optimization</td><td style="text-align:right;"><code>@assert_inlined</code> (best-effort)</td></tr><tr><td style="text-align:right;"><strong>A whole kernel that must stay on the fast path</strong></td><td style="text-align:right;">any of the above, anywhere in the call</td><td style="text-align:right;"><code>@strict</code> (combines the per-call guarantees)</td></tr><tr><td style="text-align:right;"><strong>A <code>@generated</code>/SIMD kernel that must vectorize and stay on the fast path</strong></td><td style="text-align:right;">silent ~100× regression from boxing, or vectorization silently disabled — easy to miss during exploration</td><td style="text-align:right;"><code>@kernel</code> (bundles <code>@assert_noalloc</code> + <code>@assert_vectorized</code> + <code>@assert_typestable</code>; makes the boxing check reflexive)</td></tr><tr><td style="text-align:right;"><strong>A function that must <em>never</em> regress</strong></td><td style="text-align:right;">a future edit reintroduces a trap</td><td style="text-align:right;"><code>@strict_function</code> (fails at precompile / load)</td></tr><tr><td style="text-align:right;"><strong>An interface whose implementations must be fast</strong></td><td style="text-align:right;">a new impl is correct but slow</td><td style="text-align:right;"><code>@strict_contract</code> + <code>@verify_strict</code></td></tr></tbody></table><h2 id="Patterns" tabindex="-1">Patterns <a class="header-anchor" href="#Patterns" aria-label="Permalink to &quot;Patterns {#Patterns}&quot;">​</a></h2><h3 id="Guard-a-hot-call" tabindex="-1">Guard a hot call <a class="header-anchor" href="#Guard-a-hot-call" aria-label="Permalink to &quot;Guard a hot call {#Guard-a-hot-call}&quot;">​</a></h3><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@strict</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> kernel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b)        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># type-stable + non-allocating, or it throws; returns the value</span></span></code></pre></div><h3 id="Lock-in-a-definition&#39;s-contract" tabindex="-1">Lock in a definition&#39;s contract <a class="header-anchor" href="#Lock-in-a-definition&#39;s-contract" aria-label="Permalink to &quot;Lock in a definition&#39;s contract {#Lock-in-a-definition&#39;s-contract}&quot;">​</a></h3><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@strict_function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> axpy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Float64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NTuple{4,Float64}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NTuple{4,Float64}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> y</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># A later edit that makes this allocate or go unstable now breaks module load — not a profiler run.</span></span></code></pre></div><h3 id="Empirical-fallback-when-static-analysis-can&#39;t-prove-it" tabindex="-1">Empirical fallback when static analysis can&#39;t prove it <a class="header-anchor" href="#Empirical-fallback-when-static-analysis-can&#39;t-prove-it" aria-label="Permalink to &quot;Empirical fallback when static analysis can&#39;t prove it {#Empirical-fallback-when-static-analysis-can&#39;t-prove-it}&quot;">​</a></h3><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_noalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> static </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> stream_step!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buf, x)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># measures @allocated after a warmup</span></span></code></pre></div><h3 id="Choose-error-vs.-warn" tabindex="-1">Choose error vs. warn <a class="header-anchor" href="#Choose-error-vs.-warn" aria-label="Permalink to &quot;Choose error vs. warn {#Choose-error-vs.-warn}&quot;">​</a></h3><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">StrictMode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">enable_checks!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(fail_mode </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;warn&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># log violations instead of throwing</span></span></code></pre></div><h3 id="Trade-rigor-for-speed-while-iterating" tabindex="-1">Trade rigor for speed while iterating <a class="header-anchor" href="#Trade-rigor-for-speed-while-iterating" aria-label="Permalink to &quot;Trade rigor for speed while iterating {#Trade-rigor-for-speed-while-iterating}&quot;">​</a></h3><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">StrictMode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">enable_checks!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(analysis </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;fast&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># cheap inference-only checks (sub-ms); :full in CI</span></span></code></pre></div><h3 id="Diagnose-instead-of-assert" tabindex="-1">Diagnose instead of assert <a class="header-anchor" href="#Diagnose-instead-of-assert" aria-label="Permalink to &quot;Diagnose instead of assert {#Diagnose-instead-of-assert}&quot;">​</a></h3><p>When you want the reason rather than a thrown error, reach for <code>@explain</code>. It gathers <code>@code_warntype</code>, JET, and AllocCheck into one <code>StrictReport</code>, and it never throws:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@explain</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> component</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(state, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># returns a report explaining each verdict</span></span></code></pre></div><h2 id="How-each-check-works-so-you-can-trust-the-failures" tabindex="-1">How each check works (so you can trust the failures) <a class="header-anchor" href="#How-each-check-works-so-you-can-trust-the-failures" aria-label="Permalink to &quot;How each check works (so you can trust the failures) {#How-each-check-works-so-you-can-trust-the-failures}&quot;">​</a></h2><ul><li><p><strong><code>@assert_noalloc</code></strong> asks AllocCheck to <em>prove</em> the call cannot allocate. Any reported site — including dynamic dispatch and boxing — fails the guarantee. If static analysis can&#39;t run, it falls back to an empirical <code>@allocated</code> measurement after a warmup call.</p></li><li><p><strong><code>@assert_noboxing</code></strong> runs the same AllocCheck analysis but reports only the <em>boxing / dynamic-dispatch</em> subclass (<code>DynamicDispatch</code>, <code>jl_box_*</code> / <code>jl_get_nth_field_checked</code> runtime calls, <code>Core.Box</code>), so legitimate typed allocations pass.</p></li><li><p><strong><code>@assert_typestable</code></strong> combines <code>Test.@inferred</code> (the <em>return type</em> must be concrete) with <code>JET.@report_opt</code> (no <em>internal</em> instability or runtime dispatch).</p></li><li><p><strong><code>@assert_inlined</code></strong> compiles a wrapper around the call and checks its optimized IR for a surviving <code>:invoke</code> to the callee — best-effort, since inlining is a heuristic.</p></li><li><p><strong><code>@strict_function</code></strong> runs the no-alloc + concrete-return checks against the declared argument types at precompile/load time, so a violation stops the module from loading.</p></li></ul><h2 id="Force-the-fast-path" tabindex="-1">Force the fast path <a class="header-anchor" href="#Force-the-fast-path" aria-label="Permalink to &quot;Force the fast path {#Force-the-fast-path}&quot;">​</a></h2><p>Better than detecting boxing is not boxing at all. <code>@unroll</code> fully unrolls a fixed-count loop with literal indices, so a heterogeneous tuple gets indexed type-stably instead of boxing:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tuple_sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(t)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    s </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.0</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    @unroll</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        s </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> t[i]        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → s += t[1]; t[2]; t[3]   (no boxing; @assert_noalloc passes)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    end</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    s</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>For a size known only from a type, lift it with <code>staticval(n)</code> and splice the literal into <code>@unroll</code> from a <code>@generated</code> method.</p><h2 id="SIMD-kernel-workflow" tabindex="-1">SIMD kernel workflow <a class="header-anchor" href="#SIMD-kernel-workflow" aria-label="Permalink to &quot;SIMD kernel workflow {#SIMD-kernel-workflow}&quot;">​</a></h2><p>Practical guidance for SIMD/<code>@generated</code> kernel development: audit reflexes, coverage, and correctness verification against a reference.</p><h3 id="Annotate-every-hot-loop-—-not-just-the-obvious-ones" tabindex="-1">Annotate every hot loop — not just the obvious ones <a class="header-anchor" href="#Annotate-every-hot-loop-—-not-just-the-obvious-ones" aria-label="Permalink to &quot;Annotate every hot loop — not just the obvious ones {#Annotate-every-hot-loop-—-not-just-the-obvious-ones}&quot;">​</a></h3><p>StrictMode audits the kernels you point it at — it does not scan for hot loops automatically. A scalar loop in the &quot;glue&quot; between two audited kernels will not trigger any guarantee and can silently dominate runtime.</p><p>Use <code>@strict</code> or <code>@kernel</code> as you write each numeric loop, not only as a post-hoc check. When something is slow and all audited kernels pass, look at the unaudited glue between them.</p><p><a href="/StrictMode.jl/dev/api#StrictMode.@assert_no_scalar_loops"><code>@assert_no_scalar_loops</code></a> can help: it checks that a function&#39;s compiled body contains no scalar FP or integer hot loops (loop-carried <code>phi</code> with no vector ops). Apply it to any function where you expect auto-vectorization to have kicked in.</p><h3 id="Port-against-a-golden-reference" tabindex="-1">Port against a golden reference <a class="header-anchor" href="#Port-against-a-golden-reference" aria-label="Permalink to &quot;Port against a golden reference {#Port-against-a-golden-reference}&quot;">​</a></h3><p>When porting a numeric kernel from a reference implementation (Rust, C, Fortran), a layer-by-layer bit-exact comparison is the most reliable way to verify correctness and catch subtle semantic differences.</p><ol><li><p><strong>Port one layer at a time.</strong> Implement a single kernel (e.g. a Householder reflector), run it against the reference&#39;s <em>own output</em> for the same input, and assert bit-exact agreement before moving on. Do not wait until the full algorithm is assembled — errors compound.</p></li><li><p><strong>Bit-exact where possible.</strong> Pure arithmetic (multiply-add chains, memory copies, index arithmetic) can be matched exactly. Reference-output comparison caught three deviations in a QR port that a source-reading pass would have missed: a norm kernel that was single-accumulator rather than the 2-way its source appeared to be; an <code>abs2_add</code> implemented as FMA rather than <code>mul + add</code>; and a <code>hypot</code> that used a custom overflow-safe path rather than <code>libm</code>.</p></li><li><p><strong>Allow ~1 ULP for SIMD reductions.</strong> The lane-combine order for vector reductions is LLVM-codegen-defined (see <a href="./guarantees">Promise scope</a> in the Guarantees guide). Brute-forcing accumulation models showed that the last ULP of a 4-way reduction cannot always be reproduced cross-codegen. Use a tolerance of 1–2 ULP for reduction-shaped operations; require exact match for everything else.</p></li><li><p><strong>Keep the harness alive.</strong> The bit-exact tests become a regression suite. A later optimization that shifts a value beyond tolerance is a real signal worth investigating.</p></li></ol><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># bit-exact check for a deterministic kernel</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> my_norm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ref_norm</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># tolerance for a SIMD reduction</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> abs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">my_dot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ref_dot) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">≤</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> eps</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ref_dot)</span></span></code></pre></div><p>StrictMode provides <a href="/StrictMode.jl/dev/api#StrictMode.@golden"><code>@golden</code></a> for this pattern. Record mode writes a typed golden file; compare mode does exact or ULP-tolerant comparison and throws <code>StrictViolation</code> on mismatch:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@golden</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> norm_check </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">my_norm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x)            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># exact comparison (deterministic kernel)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@golden</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> dot_check </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">my_dot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b) ulps</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # tolerance-aware (SIMD reduction)</span></span></code></pre></div><p>For problems with multiple valid outputs (e.g. &quot;any shortest round-trip decimal&quot;), pass a <code>validator=</code> predicate instead of a golden file:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@golden</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ryu_check </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">ryu_format</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x) validator </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> s </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> parse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Float64, s) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x</span></span></code></pre></div><h3 id="Guarantee-the-kernel,-smoke-test-the-entry" tabindex="-1">Guarantee the kernel, smoke-test the entry <a class="header-anchor" href="#Guarantee-the-kernel,-smoke-test-the-entry" aria-label="Permalink to &quot;Guarantee the kernel, smoke-test the entry {#Guarantee-the-kernel,-smoke-test-the-entry}&quot;">​</a></h3><p>Public functions often can&#39;t carry whole-method guarantees:</p><ul><li><p><strong>Union-returning entries</strong>: a function returning <code>Union{Int,Nothing}</code> — the canonical Julia idiom for &quot;index or not found&quot; (<code>findfirst</code>, <code>iterate</code>, <code>tryparse</code>). <code>@assert_typestable</code> accepts small isbits unions like this, so this case is handled directly.</p></li><li><p><strong>Base-delegating entries</strong>: the public wrapper forwards edge cases to Base (e.g. <code>length(needle) ≤ 1 &amp;&amp; return findfirst(...)</code>), which may allocate or dispatch dynamically across the full call graph. Whole-method <code>@assert_noalloc</code> can&#39;t be placed on such entries.</p></li></ul><p>The pattern for Base-delegating entries: assert on the <strong>inner kernel</strong> (concrete return, alloc-free), and empirically smoke-test the <strong>public entry</strong> with a runtime <code>@allocated</code> check:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Inner pointer kernel: concrete return, fully assertable</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@kernel</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _find_substr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ph, lh, pn, ln)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># noalloc + vectorized + typestable</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Public entry: delegates to Base for edge cases — empirical check only</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> @allocated</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">find_substr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(haystack, needle)) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # runtime zero-alloc on the hot path</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> find_substr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(haystack, needle) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> expected         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># correctness</span></span></code></pre></div><p>The pattern: assert on the leaf kernel where the guarantees actually hold; the thin dispatcher is smoke-tested empirically. The audit covers the performance-critical path; edge-case branches stay outside the guarantee boundary.</p><h3 id="Defeat-dead-code-elimination-before-measuring" tabindex="-1">Defeat dead-code elimination before measuring <a class="header-anchor" href="#Defeat-dead-code-elimination-before-measuring" aria-label="Permalink to &quot;Defeat dead-code elimination before measuring {#Defeat-dead-code-elimination-before-measuring}&quot;">​</a></h3><p>A benchmark that only observes a derived value (a length, a checksum) lets the optimizer eliminate the actual work — the timing then measures the derived value alone, not the kernel.</p><p>The rule: <strong>every measured kernel needs an explicit sink that consumes its output.</strong></p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Wrong: only length observed — stores may be DCE&#39;d</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@btime</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">format_int!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buf, x))</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Right: sink the buffer so the stores are required</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@btime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">format_int!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buf, x); Base</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">donotdelete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buf))</span></span></code></pre></div><p>If benchmarking against a reference implementation, ensure the reference also sinks its output (e.g. <code>std::hint::black_box(buf)</code> in Rust, <code>volatile</code> write in C). <code>Base.donotdelete</code> is available in Julia 1.8+.</p><h3 id="Measure-across-representative-value-classes" tabindex="-1">Measure across representative value classes <a class="header-anchor" href="#Measure-across-representative-value-classes" aria-label="Permalink to &quot;Measure across representative value classes {#Measure-across-representative-value-classes}&quot;">​</a></h3><p>A single benchmark input can be misleading when the kernel does different amounts of work depending on the value — a formatter, search function, or compression codec may run several times faster on &quot;easy&quot; inputs than on &quot;hard&quot; ones. A one-distribution number is not a verdict.</p><p>Required: measure across the value classes your kernel will actually see, and report the spread:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (label, gen) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rand&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()), (</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;randn&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">randn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()), (</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;integer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Float64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">^</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)))]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> @belapsed</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> kernel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">$</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">gen</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) setup</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">nothing</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    println</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">$label</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">$(round(t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1e9, digits</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1))</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ns&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>If the spread exceeds 2×, the &quot;typical case&quot; number may not represent production load.</p>`,53)])])}const g=e(n,[["render",l]]);export{k as __pageData,g as default};
