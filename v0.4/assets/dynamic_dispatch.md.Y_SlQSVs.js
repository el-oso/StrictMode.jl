import{_ as e,o as a,c as i,ao as s}from"./chunks/framework.DxAP7iL2.js";const g=JSON.parse('{"title":"Dynamic dispatch","description":"","frontmatter":{},"headers":[],"relativePath":"dynamic_dispatch.md","filePath":"dynamic_dispatch.md","lastUpdated":null}'),n={name:"dynamic_dispatch.md"};function d(o,t,l,r,h,c){return a(),i("div",null,[...t[0]||(t[0]=[s(`<h1 id="dynamic-dispatch" tabindex="-1">Dynamic dispatch <a class="header-anchor" href="#dynamic-dispatch" aria-label="Permalink to &quot;Dynamic dispatch {#dynamic-dispatch}&quot;">​</a></h1><p>A dynamic dispatch is a call Julia resolves at run time instead of at compile time. It buys flexibility and costs static resolution: no inlining across it, no constant folding through it, a boxed result, and <code>juliac --trim=safe</code> rejects it. Outside a hot loop that trade is usually fine. Inside one it is expensive, and the numbers below say how expensive.</p><p>This page explains when a call goes dynamic, how to see it (<a href="/StrictMode.jl/v0.4/api#StrictMode.dispatch_report"><code>dispatch_report</code></a>), and which fixes actually work. Every figure here was measured on Julia 1.13.0; the fixtures live in <code>StrictMode/test/dispatch_test.jl</code>.</p><h2 id="The-cost,-measured" tabindex="-1">The cost, measured <a class="header-anchor" href="#The-cost,-measured" aria-label="Permalink to &quot;The cost, measured {#The-cost,-measured}&quot;">​</a></h2><p>The same work over 1000 elements, dispatching once per element:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Container</th><th style="text-align:right;">Time</th><th style="text-align:right;">Allocated</th></tr></thead><tbody><tr><td style="text-align:right;"><code>Vector{Op}</code> — abstract element type</td><td style="text-align:right;">24.02 µs</td><td style="text-align:right;">32 000 B</td></tr><tr><td style="text-align:right;"><code>Vector{Union{O1,O2,O3,O4}}</code></td><td style="text-align:right;"><strong>0.58 µs</strong></td><td style="text-align:right;"><strong>0 B</strong></td></tr><tr><td style="text-align:right;"><code>Vector{O1}</code> — concrete</td><td style="text-align:right;"><strong>0.58 µs</strong></td><td style="text-align:right;"><strong>0 B</strong></td></tr></tbody></table><p>A <code>Union</code> element type is as fast as a concrete one here, because inference enumerates the members and emits one branch each. That is <em>union splitting</em>, and it is static code.</p><p>One dispatch is cheap. A thousand are not: dispatching once and then looping inside a specialized method measured no slower than a fully static loop.</p><h2 id="What-makes-a-call-dynamic" tabindex="-1">What makes a call dynamic <a class="header-anchor" href="#What-makes-a-call-dynamic" aria-label="Permalink to &quot;What makes a call dynamic {#What-makes-a-call-dynamic}&quot;">​</a></h2><p>Inference resolves a call while at most <code>max_methods</code> methods match it — <strong>3</strong> by default, from <code>Base.Compiler.InferenceParams()</code>. The fourth matching method turns the call dynamic:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Situation</th><th style="text-align:right;">Result</th><th style="text-align:right;">Return type</th></tr></thead><tbody><tr><td style="text-align:right;">Argument typed as a <code>Union</code> of concrete types (2–6 members tested)</td><td style="text-align:right;">union split, static</td><td style="text-align:right;">concrete</td></tr><tr><td style="text-align:right;">Argument typed abstractly, <strong>1–3</strong> methods match</td><td style="text-align:right;">static, branches inlined</td><td style="text-align:right;">concrete</td></tr><tr><td style="text-align:right;">Argument typed abstractly, <strong>4+</strong> methods match</td><td style="text-align:right;"><strong>dynamic dispatch</strong></td><td style="text-align:right;"><code>Any</code></td></tr><tr><td style="text-align:right;">The same 4-method case with <code>Base.Experimental.@max_methods 4</code> in the module</td><td style="text-align:right;">static again</td><td style="text-align:right;">concrete</td></tr></tbody></table><p>Two consequences are worth knowing before reading any finding.</p><p><strong>The declaration is not the cause.</strong> <code>f(x::Op)</code> called with a concrete <code>O1</code> is specialized and static — Julia specializes on the argument&#39;s <em>actual</em> type. Instability comes from a value whose type the caller does not know: an element of an abstractly typed container, an abstract field, or a non-<code>const</code> global.</p><p><strong>The cliff is non-local.</strong> A call matching exactly 3 methods is static today. A fourth method added <em>anywhere in any package</em> makes it dynamic, with no edit to the calling code. <a href="/StrictMode.jl/v0.4/api#StrictMode.dispatch_report"><code>dispatch_report</code></a> lists those call sites under &quot;AT THE LIMIT&quot; for exactly this reason.</p><h2 id="The-causes-StrictMode-names" tabindex="-1">The causes StrictMode names <a class="header-anchor" href="#The-causes-StrictMode-names" aria-label="Permalink to &quot;The causes StrictMode names {#The-causes-StrictMode-names}&quot;">​</a></h2><p>A <code>:typestable</code> finding names the declaration to edit, not just that the body is unstable:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Cause</th><th style="text-align:right;">What it means</th><th style="text-align:right;">Fix</th></tr></thead><tbody><tr><td style="text-align:right;"><code>captured_variable</code></td><td style="text-align:right;">a closure captured a reassigned local, so it lives in a <code>Core.Box</code> and every read is <code>Any</code></td><td style="text-align:right;">assign once, bind a fresh one with <code>let</code>, or hold the state in a typed <code>Ref</code></td></tr><tr><td style="text-align:right;"><code>nonconst_global</code></td><td style="text-align:right;">reads an untyped non-<code>const</code> global, which infers as <code>Any</code></td><td style="text-align:right;">make it <code>const</code>; for changing state, <code>const x = Ref(v)</code> or a typed binding <code>global x::Int</code></td></tr><tr><td style="text-align:right;"><code>abstract_field</code></td><td style="text-align:right;">reads a field declared with an abstract type, including <code>::Function</code></td><td style="text-align:right;">parametrize the struct: <code>struct S{T}; x::T; end</code></td></tr><tr><td style="text-align:right;"><code>abstract_eltype</code></td><td style="text-align:right;">a payload of an abstractly typed container flows into the body — <code>Vector{Any}</code>, <code>Dict{String,Any}</code>, <code>Ref{Any}</code>, a loosely typed <code>Tuple</code></td><td style="text-align:right;">give the container a concrete payload type, or a <code>Union</code> of the concrete types it holds</td></tr><tr><td style="text-align:right;"><code>reflection</code></td><td style="text-align:right;">calls <code>return_types</code>, <code>invokelatest</code>, <code>which</code>, <code>methods</code></td><td style="text-align:right;">compute the type at the call site, or move the query to load time</td></tr><tr><td style="text-align:right;"><code>runtime_type_parameter</code></td><td style="text-align:right;">builds a type from a value known only at run time (<code>ntuple(f, n)</code>, <code>Val(n)</code>)</td><td style="text-align:right;">push that value into the type domain at the boundary</td></tr><tr><td style="text-align:right;"><code>method_count</code></td><td style="text-align:right;">more methods match the call than <code>max_methods</code></td><td style="text-align:right;">fewer methods, a <code>Union</code> of concrete types, or raise the limit where the callee lives</td></tr></tbody></table><p>Order matters, because instability propagates. A widened global makes every downstream call match a huge method set, so the global is named and the method count is not: the first cause reported is the root, not its symptoms. A cause is reported only for a body that is actually unstable — declaring <code>f(v::Vector{Real})</code> and calling <code>length(v)</code> is perfectly stable, and gets no finding and no cause.</p><h2 id="Raised-limits-are-honored" tabindex="-1">Raised limits are honored <a class="header-anchor" href="#Raised-limits-are-honored" aria-label="Permalink to &quot;Raised limits are honored {#Raised-limits-are-honored}&quot;">​</a></h2><p><code>Base.Experimental.@max_methods N</code> can raise the limit for a module or a function. <a href="/StrictMode.jl/v0.4/api#StrictMode.dispatch_report"><code>dispatch_report</code></a> reads the limit that applies to each callee, so a four-method call in a module that raised the limit to four is reported as <em>at the limit</em>, not as dynamic — and a three-method one there is not flagged at all.</p><h2 id="What-does-not-work" tabindex="-1">What does not work <a class="header-anchor" href="#What-does-not-work" aria-label="Permalink to &quot;What does not work {#What-does-not-work}&quot;">​</a></h2><p>Two fixes that sound right and are not:</p><ul><li><p><strong><code>f(s::S) where {S&lt;:Shape}</code></strong> — measured: the return type stayed <code>Any</code> and the call stayed dynamic. A type parameter cannot recover a type the caller never had.</p></li><li><p><strong>A function barrier, as a stability fix</strong> — the outer frame still returns <code>Any</code> and still holds one dynamic call. A barrier is worth it to stop dispatching <em>per element</em>, not to make the caller stable, and a dynamic call already lands in a specialized method, so the gain is only where the dispatch would otherwise repeat.</p></li></ul><h2 id="Trim-compatibility" tabindex="-1">Trim compatibility <a class="header-anchor" href="#Trim-compatibility" aria-label="Permalink to &quot;Trim compatibility {#Trim-compatibility}&quot;">​</a></h2><p><code>:trim_compatible</code> is stricter than speed. juliac&#39;s verifier rejects a call it cannot resolve, and it also rejects a call whose union split exceeds its reachability limit (<code>max_union_splitting</code>, 4). So a wide <code>Union</code> can fix speed and still fail a trimmed build. For code that must trim, prefer a concrete element type, or keep the <code>Union</code> small.</p><h2 id="Seeing-where-you-stand" tabindex="-1">Seeing where you stand <a class="header-anchor" href="#Seeing-where-you-stand" aria-label="Permalink to &quot;Seeing where you stand {#Seeing-where-you-stand}&quot;">​</a></h2><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;" class="repl-prompt repl-prompt-julia">julia</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;" class="repl-prompt repl-prompt-julia">&gt;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> StrictMode</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;" class="repl-prompt repl-prompt-julia">julia</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;" class="repl-prompt repl-prompt-julia">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> dispatch_report</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(MyPkg)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">MyPkg — dispatch (max_methods </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">DYNAMIC (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  total</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Vector{Shape})       shapes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">jl</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    area — </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> methods match, limit </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">AT THE LIMIT (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  measure</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Vector{Shape})     shapes</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">jl</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">22</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    area — </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> methods match, limit </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> — one more flips it</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CLEAR</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 14</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> specialization</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(s) with no dynamic or at</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">the</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">limit call</span></span></code></pre></div><p>The sweep covers what has actually compiled, exactly like <a href="/StrictMode.jl/v0.4/api#StrictMode.audit"><code>audit</code></a><code>(mod; sweep = true)</code>. A function called only with concrete arguments is reported as clear, because that is what its compiled code does.</p><h2 id="In-an-audit" tabindex="-1">In an audit <a class="header-anchor" href="#In-an-audit" aria-label="Permalink to &quot;In an audit {#In-an-audit}&quot;">​</a></h2><p><code>audit(MyPkg; sweep = true)</code> carries the same sites as informational findings (<code>guarantee = :dispatch</code>, <code>status = :info</code>), so an agent or a CI job reads them as JSON alongside the guarantees. They never count as failures: a dynamic call may be exactly what you intended, and an at-the-limit call is not wrong at all today — but neither is visible any other way, which is why they are on by default. Pass <code>dispatch_suggest = false</code> to skip the pass; it costs one extra typed-IR read per specialization (17.8% of a 10,974-specialization sweep, which reported no findings at all).</p>`,30)])])}const y=e(n,[["render",d]]);export{g as __pageData,y as default};
