import{_ as e,o as a,c as i,ao as t}from"./chunks/framework.D1jajXEQ.js";const k=JSON.parse('{"title":"Getting Started","description":"","frontmatter":{},"headers":[],"relativePath":"getting_started.md","filePath":"getting_started.md","lastUpdated":null}'),n={name:"getting_started.md"};function h(l,s,o,p,r,d){return a(),i("div",null,[...s[0]||(s[0]=[t(`<h1 id="Getting-Started" tabindex="-1">Getting Started <a class="header-anchor" href="#Getting-Started" aria-label="Permalink to &quot;Getting Started {#Getting-Started}&quot;">​</a></h1><h2 id="Installation" tabindex="-1">Installation <a class="header-anchor" href="#Installation" aria-label="Permalink to &quot;Installation {#Installation}&quot;">​</a></h2><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Pkg</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Pkg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;StrictMode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>StrictMode</code> reads inferred types and typed IR. It needs no other package and is fast enough to run at load time. Where it is <strong>guessing</strong> — the allocation and trim checks — it warns instead of failing your build. Where it reads the compiled output and knows, it throws.</p><p>The proofs are heavy, so they live in a second package you add to your test environment only: <a href="https://github.com/JuliaLang/AllocCheck.jl" target="_blank" rel="noreferrer">AllocCheck.jl</a>, <a href="https://github.com/aviatesk/JET.jl" target="_blank" rel="noreferrer">JET.jl</a> and TrimCheck.jl.</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># in test/Project.toml, alongside StrictMode:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Pkg</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">add</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;StrictModeTest&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p><code>StrictModeTest</code> <strong>gates</strong>: <code>@test_noalloc</code>, <code>@test_typestable</code> and the <code>test_signatures</code> / <code>test_compiled</code> / <code>test_registered</code> drivers all throw. The macro you write picks the engine — nothing else does. <code>proofs_loaded()</code> says which one a session has.</p><p>Code that only depends on StrictMode needs neither. For the live loop, add <code>Revise</code>.</p><h2 id="Enable-the-checks" tabindex="-1">Enable the checks <a class="header-anchor" href="#Enable-the-checks" aria-label="Permalink to &quot;Enable the checks {#Enable-the-checks}&quot;">​</a></h2><p>Checks are <strong>on by default</strong>, so a dev or test environment needs no setup. That matters: with checks off every <code>@assert_*</code> is a no-op, and a suite full of them passes while testing nothing.</p><p>Turn them off for a shipped application in the deployed <code>Project.toml</code> (or a <code>LocalPreferences.toml</code> next to it):</p><div class="language-toml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">preferences</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">StrictMode</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">checks_enabled = </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span></code></pre></div><p>With it off, the macros expand to the bare call and there&#39;s nothing left to run.</p><p>Restart Julia after changing it. The setting is read when the package compiles, not while a session runs.</p><p>Start your strict tests with <a href="/StrictMode.jl/v0.4/api#StrictMode.assert_enabled"><code>assert_enabled</code></a>. It returns <code>checks_enabled()</code> locally and <strong>errors under CI</strong> when checks are off, so a disarmed suite cannot pass quietly.</p><p>For interactive use, <code>disable_checks!()</code>/<code>enable_checks!()</code> write the setting for you:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> StrictMode</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">StrictMode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">disable_checks!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># writes the setting; restart Julia to apply</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ... run without the guarantees ...</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">StrictMode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">enable_checks!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># back on</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">Why the restart?</p><p>Disabled checks compile away to nothing, so the setting has to be fixed before the package compiles. Call <code>disable_checks!()</code> and keep asserting in the same session and every check still runs — that image is already built.</p></div><p>Check the current state at any time:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> StrictMode</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">StrictMode</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">checks_enabled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">true</span></span></code></pre></div><p>(These docs are built with the checks enabled, so every live example below is really running the analysis as you read it.)</p><h2 id="Your-first-guarantee" tabindex="-1">Your first guarantee <a class="header-anchor" href="#Your-first-guarantee" aria-label="Permalink to &quot;Your first guarantee {#Your-first-guarantee}&quot;">​</a></h2><p>Start by asking a call to be allocation-free. The macro hands back the call&#39;s own value, so you can wrap any expression and carry on as if it weren&#39;t there:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">square_sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(xs) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">^</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, xs)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_noalloc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> square_sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">14.0</span></span></code></pre></div><p>Or ask for type stability:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">affine</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_typestable</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> affine</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">7.0</span></span></code></pre></div><p>Or ask for everything at once with <a href="/StrictMode.jl/v0.4/api#StrictMode.@strict"><code>@strict</code></a>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">weighted</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.5</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">b</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">y </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> @strict</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> weighted</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">4.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">3.0</span></span></code></pre></div><h2 id="The-other-half:-proving-it" tabindex="-1">The other half: proving it <a class="header-anchor" href="#The-other-half:-proving-it" aria-label="Permalink to &quot;The other half: proving it {#The-other-half:-proving-it}&quot;">​</a></h2><p>The allocation check above <strong>reports</strong>. <code>@assert_noalloc</code> reads typed IR, which still shows allocations LLVM later deletes — so it can only say something looks wrong, and a guess must not break your build.</p><p>To make the same property fail CI, use the <code>StrictModeTest</code> macro. Same question, asked of AllocCheck, and it throws:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> StrictMode, StrictModeTest</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_noalloc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> square_sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># warns — the scan&#39;s opinion</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@test_noalloc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">   square_sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># throws — AllocCheck&#39;s proof, over every path</span></span></code></pre></div><p>That pairing is what to take from this page:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>  your Project.toml          your test/Project.toml</span></span>
<span class="line"><span>  ─────────────────          ─────────────────────</span></span>
<span class="line"><span>  StrictMode                 StrictMode + StrictModeTest</span></span>
<span class="line"><span>  @assert_noalloc            @test_noalloc</span></span>
<span class="line"><span>  reads typed IR             asks AllocCheck</span></span>
<span class="line"><span>  warns  ⚠                   throws  ✗</span></span></code></pre></div><p>The macro you write picks the column. There is no mode to switch and no environment variable. <code>@assert_*</code> while you iterate, <code>@test_*</code> in the test suite.</p><p>See <a href="./proof_tier">StrictModeTest</a> for the full proving surface.</p><h2 id="When-a-guarantee-fails" tabindex="-1">When a guarantee fails <a class="header-anchor" href="#When-a-guarantee-fails" aria-label="Permalink to &quot;When a guarantee fails {#When-a-guarantee-fails}&quot;">​</a></h2><p>A guarantee that gates throws a <a href="/StrictMode.jl/v0.4/api#StrictMode.StrictViolation"><code>StrictViolation</code></a> naming the call and what went wrong. Whether a guarantee throws or warns is fixed per guarantee, not configured — the ones that observe compiled output throw, the ones that infer only warn. <a href="./guarantees">Guarantees</a> lists which is which.</p><p>Indexing a heterogeneous tuple with a runtime value is the classic case: it returns a <code>Union</code> and boxes behind your back. Here the silence becomes an error:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">state </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;three&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">component</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(s, i) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> s[i]</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_typestable</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> component</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(state, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ERROR: StrictViolation (@typestable): guarantee not satisfied</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   target:  component(state, rand(1:3))</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   reason:  return type is not concrete or isbits-union (inference): Union{Float64, Int64, String}</span></span></code></pre></div><h2 id="Zero-cost-when-disabled" tabindex="-1">Zero cost when disabled <a class="header-anchor" href="#Zero-cost-when-disabled" aria-label="Permalink to &quot;Zero cost when disabled {#Zero-cost-when-disabled}&quot;">​</a></h2><p>With the checks off, the macro expands to exactly the bare call. There&#39;s nothing left to run, and nothing to pay for:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># checks OFF</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@macroexpand</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> @assert_noalloc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> f</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#  -&gt;  :(f(x))</span></span></code></pre></div><h2 id="What-the-checks-cost-when-enabled" tabindex="-1">What the checks cost when enabled <a class="header-anchor" href="#What-the-checks-cost-when-enabled" aria-label="Permalink to &quot;What the checks cost when enabled {#What-the-checks-cost-when-enabled}&quot;">​</a></h2><p><code>StrictMode</code> has no analysis backend and no warmup step. It reads Base&#39;s own inference and nothing else, so it costs about what any small package costs to load. With checks off the macros are bare calls, so a shipped application pays nothing at all.</p><p><code>StrictModeTest</code> is where the heavy machinery is: it warms JET and AllocCheck at its own precompile. That is why it belongs in <code>test/</code> and not in your <code>Project.toml</code>.</p><p>The per-check cost is in the table below. It grows with the size of the call graph, so these checks are happiest pointed at small hot kernels — which is where the silent traps live anyway. With Revise, an edit-and-rerun loop keeps the image warm between edits.</p><h3 id="The-two-analysis-engines" tabindex="-1">The two analysis engines <a class="header-anchor" href="#The-two-analysis-engines" aria-label="Permalink to &quot;The two analysis engines {#The-two-analysis-engines}&quot;">​</a></h3><p><code>@assert_*</code> runs the cheap value-free engine; <code>@test_*</code> runs the proof. There is no rigor/speed preference to set and no ambient state to read — the macro name selects the engine.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Package</th><th style="text-align:right;">Macro</th><th style="text-align:right;">Type stability</th><th style="text-align:right;">No-allocation / no-boxing</th><th style="text-align:right;">Per-method cost</th><th style="text-align:right;">On a violation</th></tr></thead><tbody><tr><td style="text-align:right;"><code>StrictMode</code></td><td style="text-align:right;"><code>@assert_*</code></td><td style="text-align:right;"><code>Base.return_types</code> concreteness + an IR boxing signal</td><td style="text-align:right;"><code>code_typed</code> IR + <code>infer_effects</code> scan</td><td style="text-align:right;">~70 µs</td><td style="text-align:right;">reports</td></tr><tr><td style="text-align:right;"><code>StrictModeTest</code></td><td style="text-align:right;"><code>@test_*</code></td><td style="text-align:right;">JET <code>@report_opt</code></td><td style="text-align:right;">AllocCheck static proof</td><td style="text-align:right;">~900 µs</td><td style="text-align:right;">throws</td></tr></tbody></table><p><code>StrictMode</code>&#39;s engine is a quick triage over all the properties at once, type stability as well as allocation and boxing, built entirely on Base&#39;s own inference. Because of that it needs no AllocCheck or JET and runs roughly 10× cheaper per method than the proof (see <code>StrictMode/bench/timetax.jl</code>). It catches the usual suspects, like explicit heap allocation, boxing, dynamic dispatch, and non-concrete returns. Being a scan of typed IR it can miss or over-flag something that AllocCheck&#39;s LLVM-level proof would get exactly right — which is exactly why it reports rather than gating. The split that works well in practice: <code>StrictMode</code>&#39;s <code>@assert_*</code> while you iterate, <code>StrictModeTest</code>&#39;s <code>@test_*</code> and <code>test_*</code> drivers in CI.</p><p>Two guarantees are graded per layer rather than per package. <code>@assert_typestable</code>&#39;s return-type concreteness is exact for the question it asks, so it throws; its IR boxing signal is a heuristic, so that layer warns. And <code>@assert_memsafe</code>, <code>@assert_vectorized</code>, <code>@assert_no_spill</code> and friends all throw, because they <em>observe</em> compiled output rather than inferring about it.</p><h3 id="Incremental-re-checks" tabindex="-1">Incremental re-checks <a class="header-anchor" href="#Incremental-re-checks" aria-label="Permalink to &quot;Incremental re-checks {#Incremental-re-checks}&quot;">​</a></h3><p><code>findings</code> and <code>audit</code> cache their results per <code>(method, world, signature, guarantees)</code>. A re-run only re-analyzes the methods that actually changed, so editing one method and running <code>audit</code> again comes back almost instantly while everything else is a cache hit. <a href="/StrictMode.jl/v0.4/api#StrictMode.cache_stats"><code>cache_stats</code></a> shows you the hits and misses, and <a href="/StrictMode.jl/v0.4/api#StrictMode.clear_cache!"><code>clear_cache!</code></a> is there for the one case the cache can&#39;t see: when you edit a <em>callee</em> of a checked method rather than the method itself.</p><p>From here, the <a href="./guarantees">Guarantees</a> guide walks through each macro in turn, with examples you can run.</p>`,59)])])}const g=e(n,[["render",h]]);export{k as __pageData,g as default};
