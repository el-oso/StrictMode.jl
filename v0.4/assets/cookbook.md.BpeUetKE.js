import{_ as i,o as a,c as e,ao as t}from"./chunks/framework.D1jajXEQ.js";const c=JSON.parse('{"title":"StrictMode cookbook: trap → macro","description":"","frontmatter":{},"headers":[],"relativePath":"cookbook.md","filePath":"cookbook.md","lastUpdated":null}'),n={name:"cookbook.md"};function l(h,s,o,r,p,d){return a(),e("div",null,[...s[0]||(s[0]=[t(`<h1 id="StrictMode-cookbook:-trap-macro" tabindex="-1">StrictMode cookbook: trap → macro <a class="header-anchor" href="#StrictMode-cookbook:-trap-macro" aria-label="Permalink to &quot;StrictMode cookbook: trap → macro {#StrictMode-cookbook:-trap-macro}&quot;">​</a></h1><p>A quick lookup table from the usual Julia performance traps to the StrictMode guarantee that catches each one. Checks are on by default; <code>StrictMode.disable_checks!()</code> compiles them away to nothing for a shipped application.</p><table tabindex="0"><thead><tr><th style="text-align:right;">Performance trap</th><th style="text-align:right;">Symptom</th><th style="text-align:right;">Catch it with</th></tr></thead><tbody><tr><td style="text-align:right;"><strong>Runtime tuple indexing</strong> (<code>t[i]</code>, <code>t</code> heterogeneous, <code>i</code> a runtime value)</td><td style="text-align:right;"><code>Union</code> return type, silent boxing</td><td style="text-align:right;"><code>@assert_noboxing</code> / <code>@unroll</code> to fix</td></tr><tr><td style="text-align:right;"><strong>Type-unstable return</strong> (a branch returns <code>Int</code>, another <code>Float64</code>)</td><td style="text-align:right;"><code>Union{...}</code>/<code>Any</code> return; downstream boxing</td><td style="text-align:right;"><code>@assert_typestable</code></td></tr><tr><td style="text-align:right;"><strong>Captured-variable boxing</strong> (a closure mutates an outer local)</td><td style="text-align:right;"><code>Core.Box</code>, allocations, lost inference</td><td style="text-align:right;"><code>@assert_noboxing</code></td></tr><tr><td style="text-align:right;"><strong>Untyped accumulator</strong> (<code>acc = []</code> / <code>acc = 0</code> later holding mixed types)</td><td style="text-align:right;">per-iteration allocation, dispatch</td><td style="text-align:right;"><code>@assert_noalloc</code> / <code>@strict</code></td></tr><tr><td style="text-align:right;"><strong>Allocating hot loop</strong> (<code>push!</code> into a fresh <code>Vector</code>, <code>collect</code>, slices)</td><td style="text-align:right;">heap traffic, GC pressure in inner loops</td><td style="text-align:right;"><code>@assert_noalloc</code></td></tr><tr><td style="text-align:right;"><strong>Boxing, but buffers are fine</strong> (must not box, may allocate scratch space)</td><td style="text-align:right;">runtime dispatch / <code>jl_get_nth_field_checked</code> only</td><td style="text-align:right;"><code>@assert_noboxing</code> (allows typed allocations)</td></tr><tr><td style="text-align:right;"><strong>Accidental dynamic dispatch</strong> (abstract field types, <code>Any</code> args)</td><td style="text-align:right;">runtime dispatch shows as allocation</td><td style="text-align:right;"><code>@assert_noboxing</code> / <code>@assert_noalloc</code></td></tr><tr><td style="text-align:right;"><strong>A call that should inline but doesn&#39;t</strong> (cost-model misfire, <code>@noinline</code>)</td><td style="text-align:right;">call overhead, lost cross-call optimization</td><td style="text-align:right;"><code>@assert_inlined</code> (best-effort)</td></tr><tr><td style="text-align:right;"><strong>A whole kernel that must stay on the fast path</strong></td><td style="text-align:right;">any of the above, anywhere in the call</td><td style="text-align:right;"><code>@strict</code> (type stability, owned scratch, allocation-freedom)</td></tr><tr><td style="text-align:right;"><strong>A <code>@generated</code>/SIMD kernel that must vectorize and stay on the fast path</strong></td><td style="text-align:right;">silent ~100× regression from boxing, or vectorization silently disabled — easy to miss during exploration</td><td style="text-align:right;"><code>@kernel</code> (bundles <code>@assert_noalloc</code> + <code>@assert_vectorized</code> + <code>@assert_typestable</code>; makes the boxing check reflexive)</td></tr><tr><td style="text-align:right;"><strong>A function that must <em>never</em> regress</strong></td><td style="text-align:right;">a future edit reintroduces a trap</td><td style="text-align:right;"><code>@strict_function</code> (return type checked at load; allocation re-proved by <code>test_registered()</code>)</td></tr><tr><td style="text-align:right;"><strong>An interface whose implementations must be fast</strong></td><td style="text-align:right;">a new impl is correct but slow</td><td style="text-align:right;"><code>@strict_contract</code> + <code>@verify_strict</code></td></tr></tbody></table><p>Every <code>@assert_*</code> above comes from <code>StrictMode</code> and reports. Where you want the same property to fail CI, write the <code>@test_*</code> macro of the same name from <a href="./proof_tier"><code>StrictModeTest</code></a> — <code>@test_noalloc</code>, <code>@test_noboxing</code>, <code>@test_typestable</code>, <code>@test_trim_compatible</code>, <code>@test_strict</code>, <code>@test_kernel</code>.</p><h2 id="Patterns" tabindex="-1">Patterns <a class="header-anchor" href="#Patterns" aria-label="Permalink to &quot;Patterns {#Patterns}&quot;">​</a></h2><h3 id="Guard-a-hot-call" tabindex="-1">Guard a hot call <a class="header-anchor" href="#Guard-a-hot-call" aria-label="Permalink to &quot;Guard a hot call {#Guard-a-hot-call}&quot;">​</a></h3><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@strict</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> kernel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b)        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># type-stable, owned scratch, non-allocating; returns the value</span></span></code></pre></div><h3 id="Lock-in-a-definition&#39;s-contract" tabindex="-1">Lock in a definition&#39;s contract <a class="header-anchor" href="#Lock-in-a-definition&#39;s-contract" aria-label="Permalink to &quot;Lock in a definition&#39;s contract {#Lock-in-a-definition&#39;s-contract}&quot;">​</a></h3><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@strict_function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> axpy</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Float64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, x</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NTuple{4,Float64}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, y</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">NTuple{4,Float64}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    a </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.+</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> y</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># A later edit that makes this return a non-concrete type now breaks module load; one that makes</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># it allocate warns here and fails under test_registered().</span></span></code></pre></div><h3 id="Empirical-fallback-when-static-analysis-can&#39;t-prove-it" tabindex="-1">Empirical fallback when static analysis can&#39;t prove it <a class="header-anchor" href="#Empirical-fallback-when-static-analysis-can&#39;t-prove-it" aria-label="Permalink to &quot;Empirical fallback when static analysis can&#39;t prove it {#Empirical-fallback-when-static-analysis-can&#39;t-prove-it}&quot;">​</a></h3><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_noalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> static </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> false</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> stream_step!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buf, x)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># measures @allocated after a warmup</span></span></code></pre></div><h3 id="Report-while-iterating,-gate-in-CI" tabindex="-1">Report while iterating, gate in CI <a class="header-anchor" href="#Report-while-iterating,-gate-in-CI" aria-label="Permalink to &quot;Report while iterating, gate in CI {#Report-while-iterating,-gate-in-CI}&quot;">​</a></h3><p>Nothing to configure for the first half: <code>StrictMode</code>&#39;s <code>@assert_*</code> run a cheap inference-only engine (sub-ms per method, no extra dependency) and <strong>report</strong>. Add <code>StrictModeTest</code> to the test environment and write <code>@test_*</code> where you want the AllocCheck/JET proof to <strong>fail the build</strong>:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_noalloc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> kernel!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, A, B)     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># while you iterate — warns</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@test_noalloc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    kernel!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(C, A, B)    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># in CI — throws</span></span></code></pre></div><h3 id="Diagnose-instead-of-assert" tabindex="-1">Diagnose instead of assert <a class="header-anchor" href="#Diagnose-instead-of-assert" aria-label="Permalink to &quot;Diagnose instead of assert {#Diagnose-instead-of-assert}&quot;">​</a></h3><p>When you want the reason rather than a verdict, reach for <code>@explain</code>. It gathers <code>@code_warntype</code>, the inferred return type, and the typed-IR signals into one <code>StrictReport</code>, and it never throws:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@explain</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> component</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(state, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># returns a report explaining each verdict</span></span></code></pre></div><h2 id="How-each-check-works-so-you-can-trust-the-failures" tabindex="-1">How each check works (so you can trust the failures) <a class="header-anchor" href="#How-each-check-works-so-you-can-trust-the-failures" aria-label="Permalink to &quot;How each check works (so you can trust the failures) {#How-each-check-works-so-you-can-trust-the-failures}&quot;">​</a></h2><ul><li><p><strong><code>@assert_noalloc</code></strong> scans typed IR for allocation sites — including dynamic dispatch and boxing. <code>static = false</code> measures <code>@allocated</code> after a warmup instead. Both are heuristics, so it <strong>reports</strong>; <strong><code>@test_noalloc</code></strong> asks AllocCheck to <em>prove</em> the call cannot allocate on any path, and throws.</p></li><li><p><strong><code>@assert_noboxing</code></strong> flags the <em>boxing / dynamic-dispatch</em> subclass from the same scan, so legitimate typed allocations pass. <strong><code>@test_noboxing</code></strong> classifies AllocCheck&#39;s own instances (<code>DynamicDispatch</code>, <code>jl_box_*</code> / <code>jl_get_nth_field_checked</code> runtime calls, <code>Core.Box</code>) — which is where the distinction is actually proved.</p></li><li><p><strong><code>@assert_typestable</code></strong> requires a concrete inferred return type (exact, so it throws) and adds two IR signals behind a concrete return, both heuristics, so both warn: internal dynamic dispatch, and a union-typed local that boxes a member on the way in. <strong><code>@test_typestable</code></strong> replaces the first with <code>JET.@report_opt</code> and throws on it; for the second it consults the same scan, because union splitting is not dispatch and JET cannot see it.</p></li><li><p><strong><code>@assert_inlined</code></strong> compiles a wrapper around the call and checks its optimized IR for a surviving <code>:invoke</code> to the callee — best-effort, since inlining is a heuristic, but it observes compiled output rather than inferring, so it throws.</p></li><li><p><strong><code>@strict_function</code></strong> runs the same checks against the declared argument types at precompile/load time. A non-concrete return stops the module from loading; an allocation verdict only warns there, because the proof is not loadable at a package&#39;s own precompile and a guess must not break a build. The declaration is registered either way, so <code>test_registered()</code> re-proves it from <code>test/</code>.</p></li></ul><h2 id="SIMD-kernel-workflow" tabindex="-1">SIMD kernel workflow <a class="header-anchor" href="#SIMD-kernel-workflow" aria-label="Permalink to &quot;SIMD kernel workflow {#SIMD-kernel-workflow}&quot;">​</a></h2><p>Practical guidance for SIMD/<code>@generated</code> kernel development: audit reflexes, coverage, and correctness verification against a reference.</p><h3 id="Annotate-every-hot-loop-—-not-just-the-obvious-ones" tabindex="-1">Annotate every hot loop — not just the obvious ones <a class="header-anchor" href="#Annotate-every-hot-loop-—-not-just-the-obvious-ones" aria-label="Permalink to &quot;Annotate every hot loop — not just the obvious ones {#Annotate-every-hot-loop-—-not-just-the-obvious-ones}&quot;">​</a></h3><p>StrictMode audits the kernels you point it at — it does not scan for hot loops automatically. A scalar loop in the &quot;glue&quot; between two audited kernels will not trigger any guarantee and can silently dominate runtime.</p><p>Use <code>@strict</code> or <code>@kernel</code> as you write each numeric loop, not only as a post-hoc check. When something is slow and all audited kernels pass, look at the unaudited glue between them.</p><p><a href="/StrictMode.jl/v0.4/api#StrictMode.@assert_no_scalar_loops"><code>@assert_no_scalar_loops</code></a> can help: it checks that a function&#39;s compiled body contains no scalar FP or integer hot loops (loop-carried <code>phi</code> with no vector ops). Apply it to any function where you expect auto-vectorization to have kicked in.</p><h3 id="Port-against-a-golden-reference" tabindex="-1">Port against a golden reference <a class="header-anchor" href="#Port-against-a-golden-reference" aria-label="Permalink to &quot;Port against a golden reference {#Port-against-a-golden-reference}&quot;">​</a></h3><p>When porting a numeric kernel from a reference implementation (Rust, C, Fortran), a layer-by-layer bit-exact comparison is the most reliable way to verify correctness and catch subtle semantic differences. 2. <strong>Port one layer at a time.</strong> Implement a single kernel (e.g. a Householder reflector), run it against the reference&#39;s <em>own output</em> for the same input, and assert bit-exact agreement before moving on. Do not wait until the full algorithm is assembled — errors compound.</p><ol start="3"><li><p><strong>Bit-exact where possible.</strong> Pure arithmetic (multiply-add chains, memory copies, index arithmetic) can be matched exactly. Reference-output comparison caught three deviations in a QR port that a source-reading pass would have missed: a norm kernel that was single-accumulator rather than the 2-way its source appeared to be; an <code>abs2_add</code> implemented as FMA rather than <code>mul + add</code>; and a <code>hypot</code> that used a custom overflow-safe path rather than <code>libm</code>.</p></li><li><p><strong>Allow ~1 ULP for SIMD reductions.</strong> The lane-combine order for vector reductions is LLVM-codegen-defined (see <a href="./guarantees">Promise scope</a> in the Guarantees guide). Brute-forcing accumulation models showed that the last ULP of a 4-way reduction cannot always be reproduced cross-codegen. Use a tolerance of 1–2 ULP for reduction-shaped operations; require exact match for everything else.</p></li><li><p><strong>Keep the harness alive.</strong> The bit-exact tests become a regression suite. A later optimization that shifts a value beyond tolerance is a real signal worth investigating.</p></li></ol><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># bit-exact check for a deterministic kernel</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> my_norm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ref_norm</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># tolerance for a SIMD reduction</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> abs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">my_dot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ref_dot) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">≤</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> eps</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ref_dot)</span></span></code></pre></div><p>StrictMode provides <a href="/StrictMode.jl/v0.4/api#StrictMode.@golden"><code>@golden</code></a> for this pattern. Record mode writes a typed golden file; compare mode does exact or ULP-tolerant comparison and throws <code>StrictViolation</code> on mismatch:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@golden</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;norm_check&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> my_norm</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x)            </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># exact comparison (deterministic kernel)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@golden</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;dot_check&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> my_dot</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b) ulps</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # tolerance-aware (SIMD reduction)</span></span></code></pre></div><p>For problems with multiple valid outputs (e.g. &quot;any shortest round-trip decimal&quot;), pass a <code>validator=</code> predicate instead of a golden file:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@golden</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;ryu_check&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ryu_format</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(x) validator </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> s </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> parse</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Float64, s) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">===</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> x</span></span></code></pre></div><h3 id="Guarantee-the-kernel,-smoke-test-the-entry" tabindex="-1">Guarantee the kernel, smoke-test the entry <a class="header-anchor" href="#Guarantee-the-kernel,-smoke-test-the-entry" aria-label="Permalink to &quot;Guarantee the kernel, smoke-test the entry {#Guarantee-the-kernel,-smoke-test-the-entry}&quot;">​</a></h3><p>Public functions often can&#39;t carry whole-method guarantees:</p><ul><li><p><strong>Union-returning entries</strong>: a function returning <code>Union{Int,Nothing}</code> — the canonical Julia idiom for &quot;index or not found&quot; (<code>findfirst</code>, <code>iterate</code>, <code>tryparse</code>). <code>@assert_typestable</code> accepts small isbits unions like this, so this case is handled directly.</p></li><li><p><strong>Base-delegating entries</strong>: the public wrapper forwards edge cases to Base (e.g. <code>length(needle) ≤ 1 &amp;&amp; return findfirst(...)</code>), which may allocate or dispatch dynamically across the full call graph. Whole-method <code>@assert_noalloc</code> can&#39;t be placed on such entries.</p></li></ul><p>The pattern for Base-delegating entries: assert on the <strong>inner kernel</strong> (concrete return, alloc-free), and empirically smoke-test the <strong>public entry</strong> with a runtime <code>@allocated</code> check:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Inner pointer kernel: concrete return, fully assertable</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@kernel</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> _find_substr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(ph, lh, pn, ln)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># noalloc + vectorized + typestable</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Public entry: delegates to Base for edge cases — empirical check only</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> @allocated</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">find_substr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(haystack, needle)) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   # runtime zero-alloc on the hot path</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@test</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> find_substr</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(haystack, needle) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> expected         </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># correctness</span></span></code></pre></div><p>The pattern: assert on the leaf kernel where the guarantees actually hold; the thin dispatcher is smoke-tested empirically. The audit covers the performance-critical path; edge-case branches stay outside the guarantee boundary.</p><h3 id="Defeat-dead-code-elimination-before-measuring" tabindex="-1">Defeat dead-code elimination before measuring <a class="header-anchor" href="#Defeat-dead-code-elimination-before-measuring" aria-label="Permalink to &quot;Defeat dead-code elimination before measuring {#Defeat-dead-code-elimination-before-measuring}&quot;">​</a></h3><p>A benchmark that only observes a derived value (a length, a checksum) lets the optimizer eliminate the actual work — the timing then measures the derived value alone, not the kernel.</p><p>The rule: <strong>every measured kernel needs an explicit sink that consumes its output.</strong></p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Wrong: only length observed — stores may be DCE&#39;d</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@btime</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">format_int!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buf, x))</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Right: sink the buffer so the stores are required</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@btime</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">format_int!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buf, x); Base</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">donotdelete</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(buf))</span></span></code></pre></div><p>If benchmarking against a reference implementation, ensure the reference also sinks its output (e.g. <code>std::hint::black_box(buf)</code> in Rust, <code>volatile</code> write in C). <code>Base.donotdelete</code> is available in Julia 1.8+.</p><h3 id="Measure-across-representative-value-classes" tabindex="-1">Measure across representative value classes <a class="header-anchor" href="#Measure-across-representative-value-classes" aria-label="Permalink to &quot;Measure across representative value classes {#Measure-across-representative-value-classes}&quot;">​</a></h3><p>A single benchmark input can be misleading when the kernel does different amounts of work depending on the value — a formatter, search function, or compression codec may run several times faster on &quot;easy&quot; inputs than on &quot;hard&quot; ones. A one-distribution number is not a verdict.</p><p>Required: measure across the value classes your kernel will actually see, and report the spread:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (label, gen) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;rand&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()), (</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;randn&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">randn</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()), (</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;integer&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, ()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Float64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rand</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">10</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">^</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">6</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)))]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> @belapsed</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> kernel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">$</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">gen</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()) setup</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">nothing</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    println</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">$label</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">$(round(t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1e9, digits</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1))</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ns&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>If the spread exceeds 2×, the &quot;typical case&quot; number may not represent production load.</p><h2 id="@unroll-—-force-the-fast-path" tabindex="-1"><code>@unroll</code> — force the fast path <a class="header-anchor" href="#@unroll-—-force-the-fast-path" aria-label="Permalink to &quot;\`@unroll\` — force the fast path {#@unroll-—-force-the-fast-path}&quot;">​</a></h2><p>The assert macros tell you after the fact that you boxed. <a href="/StrictMode.jl/v0.4/api#StrictMode.@unroll"><code>@unroll</code></a> keeps it from happening in the first place. When a loop&#39;s trip count is known at macro time, it unrolls the loop completely and swaps the loop variable for a literal on each pass, so <code>t[i]</code> becomes <code>t[1]; t[2]; …</code>. A heterogeneous tuple then gets indexed type-stably, with no boxing. Unlike the asserts it isn&#39;t gated behind the checks flag; the unrolling always happens.</p><p>This is the trap that started the whole project. The naive loop is type-stable, returning a concrete <code>Float64</code>, and it still allocates, because the runtime tuple index boxes. It&#39;s the exact thing <code>@assert_noalloc</code> is there to catch:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">htup </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3.0f0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> naive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(t)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    acc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.0</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        acc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> t[i]          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># runtime index over a heterogeneous tuple → boxes</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    end</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> acc</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> unrolled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(t)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    acc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.0</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    @unroll</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        acc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> t[i]          </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → acc += t[1]; t[2]; t[3]   (literal, no boxing)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    end</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> acc</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">naive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(htup), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unrolled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(htup), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@allocated</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">naive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(htup)), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@allocated</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unrolled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(htup)))</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">(6.0, 6.0, 128, 0)</span></span></code></pre></div><p>Both give the same answer, but only the naive loop allocates, so the guarantee passes for the unrolled version and not the other:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_noalloc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unrolled</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(htup)</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;">6.0</span></span></code></pre></div><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_noalloc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> naive</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(htup)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># ┌ Warning: StrictViolation (@noalloc): guarantee not satisfied</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># │   reason:  allocates / boxes (value-free IR scan)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#   (@test_noalloc names the site: Allocating runtime call to &quot;jl_get_nth_field_checked&quot;)</span></span></code></pre></div><p>When the size lives only in a type, you can lift it into the type domain with <a href="/StrictMode.jl/v0.4/api#StrictMode.staticval"><code>staticval</code></a> and splice the literal into <code>@unroll</code> from a <code>@generated</code> method:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@generated</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> tuple_sum</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Tuple</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    N </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">parameters)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    quote</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        acc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> zero</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">promote_type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(t</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">parameters</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">...</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        @unroll</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:$</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">N        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># $N is a literal inside the generated body</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">            acc </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">+=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> t[i]</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        end</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        acc</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    end</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div>`,60)])])}const g=i(n,[["render",l]]);export{c as __pageData,g as default};
