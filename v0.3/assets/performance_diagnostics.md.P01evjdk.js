import{_ as i,o as e,c as a,ao as t}from"./chunks/framework.dUIs1JHb.js";const c=JSON.parse('{"title":"Performance diagnostics","description":"","frontmatter":{},"headers":[],"relativePath":"performance_diagnostics.md","filePath":"performance_diagnostics.md","lastUpdated":null}'),n={name:"performance_diagnostics.md"};function r(l,s,o,h,p,d){return e(),a("div",null,[...s[0]||(s[0]=[t(`<h1 id="Performance-diagnostics" tabindex="-1">Performance diagnostics <a class="header-anchor" href="#Performance-diagnostics" aria-label="Permalink to &quot;Performance diagnostics {#Performance-diagnostics}&quot;">​</a></h1><p>Green guarantees don&#39;t mean fast. <code>@assert_vectorized</code>, <code>@assert_noalloc</code>, and <code>@assert_typestable</code> confirm that a kernel is on the right track — vectorized, allocation-free, type-stable — but they say nothing about whether it&#39;s close to hardware capacity. This page covers the diagnostics that live in the gap between &quot;it&#39;s correct&quot; and &quot;it&#39;s fast.&quot;</p><h2 id="Checking-for-vectorization" tabindex="-1">Checking for vectorization <a class="header-anchor" href="#Checking-for-vectorization" aria-label="Permalink to &quot;Checking for vectorization {#Checking-for-vectorization}&quot;">​</a></h2><p><a href="/StrictMode.jl/v0.3/api#StrictMode.@assert_vectorized"><code>@assert_vectorized</code></a> checks whether a kernel&#39;s compiled body contains SIMD vector operations (<code>&lt;N x …&gt;</code> in the LLVM IR). It works on the explicit-vector pattern that hand-tuned numeric code actually uses — <code>SIMD.jl</code>&#39;s <code>Vec</code> with <code>vload</code>/<code>vstore</code> over a preallocated buffer:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">using</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> SIMD</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> vscale!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dst</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{Float64}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, src</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">Vector{Float64}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    @inbounds</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> i </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">length</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(src)</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        vstore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">vload</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Vec{</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,Float64}, src, i) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, dst, i)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    end</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> dst</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_vectorized</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> vscale!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dst, src)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># passes — emits &lt;8 x double&gt;</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_noalloc</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    vscale!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(dst, src)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># …and stays allocation-free</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">Target the leaf, not the dispatcher</p><p><code>@assert_vectorized</code> inspects the compiled body of the function you point it at directly. If a thin dispatcher calls vectorized kernels without inlining them, <code>@assert_vectorized</code> on the dispatcher will fail — the vector ops are in the callees, not in this body. Point it at the leaf kernels where the SIMD actually lives.</p></div><p><a href="/StrictMode.jl/v0.3/api#StrictMode.@assert_no_scalar_loops"><code>@assert_no_scalar_loops</code></a> is the flip side: it checks that a numeric function does <em>not</em> contain a scalar hot loop (a loop-carried <code>phi double</code> or <code>phi iN</code> with no vector ops). Use it on functions where you expect auto-vectorization to kick in and want to be told if it doesn&#39;t.</p><h2 id="How-fast-is-fast?-kernel_report" tabindex="-1">How fast is fast? <code>kernel_report</code> <a class="header-anchor" href="#How-fast-is-fast?-kernel_report" aria-label="Permalink to &quot;How fast is fast? \`kernel_report\` {#How-fast-is-fast?-kernel_report}&quot;">​</a></h2><p>A kernel can emit <code>&lt;8 x double&gt;</code> and still be slower than expected. The usual reason: it&#39;s moving more memory than it&#39;s computing, so the bandwidth cost dominates. <a href="/StrictMode.jl/v0.3/api#StrictMode.kernel_report"><code>kernel_report</code></a> reads the LLVM IR and computes the arithmetic intensity — the ratio of compute operations to memory operations — so you can see at a glance whether the kernel is compute-bound or memory-bound:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;" class="repl-prompt repl-prompt-julia">julia</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;" class="repl-prompt repl-prompt-julia">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> kernel_report</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(syrk_naive!, (Matrix{Float64},))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">KernelReport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> syrk_naive!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Matrix{Float64})</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  vectorized — </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`&lt;8 x&gt;\`</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  FP vector ops </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> memory vector ops </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> :</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 12</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  →</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> arithmetic intensity </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0.67</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  →</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> memory</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">bound</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> streams more than it computes. Reuse loaded vectors across more FMAs</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    (register blocking) and tile the reduction dimension (cache blocking)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span></span></code></pre></div><p>The report is advisory, not a profiler. Think of it as a quick sanity check: a kernel with low intensity is memory-bound and may benefit from data reuse strategies (register blocking, cache tiling). A high-intensity kernel is compute-bound — the bottleneck is arithmetic, not bandwidth.</p><p><code>kernel_report</code> also handles integer-SIMD kernels (byte compares, hashing, formatting). When there are no floating-point vector ops, it falls back to counting integer vector arithmetic ops instead.</p><h3 id="Size-context" tabindex="-1">Size context <a class="header-anchor" href="#Size-context" aria-label="Permalink to &quot;Size context {#Size-context}&quot;">​</a></h3><p>Low arithmetic intensity is not always a problem. If the entire working set fits in L1 or L2 cache, a memory-bound kernel can still be fast because the loads are cheap. Pass <code>working_set_bytes</code> to get a residency annotation:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kernel_report</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(my_kernel!, (Matrix{Float64},); working_set_bytes </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 64</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → &quot;L1-resident. Low intensity is acceptable at this size;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">#    memory-bound advice applies as n grows.&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">kernel_report</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(my_kernel!, (Matrix{Float64},); working_set_bytes </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 512</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> *</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 512</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># → &quot;working set spills L3 — BLIS-style packing needed for the cache-locality leg.&quot;</span></span></code></pre></div><p>The default cache thresholds (L1=32 KiB, L2=512 KiB, L3=16 MiB) match a typical desktop. Override them with <code>StrictMode._CACHE_BYTES[] = (l1 = …, l2 = …, l3 = …)</code>.</p><h3 id="Additional-signals" tabindex="-1">Additional signals <a class="header-anchor" href="#Additional-signals" aria-label="Permalink to &quot;Additional signals {#Additional-signals}&quot;">​</a></h3><p><code>kernel_report</code> also reports several supplementary signals, each advisory:</p><p><strong>Unaligned memory ops</strong> (<code>unaligned_mem_ops</code>): vector loads/stores whose alignment annotation is below the vector width. Starting a vectorized loop partway through an aligned buffer — for example, a triangular loop that starts at the diagonal — often shows up here. Misaligned loads can stall wide SIMD.</p><p><strong>Masked ops</strong> (<code>masked_mem_ops</code>): <code>@llvm.masked.*</code> intrinsics, indicating variable-length inner-loop trips (remainder handling). Prefer fixed-width tiles when possible.</p><p><strong>Branches</strong> (<code>branch_count</code>): conditional branches inside a vectorized kernel are candidates for data-dependent misprediction. A branch that is 50/50 on real data costs ~10 cycles per call regardless of vectorization. Consider <code>ifelse</code> or lookup tables for hot decisions.</p><p><strong>Serial dependency chains</strong> (<code>serial_dep_count</code>): <code>div</code>, <code>rem</code>, or <code>sqrt</code> inside a loop where the result feeds the next iteration. Each step must wait ~10–20 cycles for the previous one to finish — the loop is latency-bound regardless of vectorization or branch prediction. Break the chain by processing multiple elements per step (for example, <code>÷100</code> extracts two decimal digits at once instead of one).</p><p><strong>Missing <code>noalias</code></strong> (<code>noalias_missing_count</code>): pointer parameters without the <code>noalias</code> attribute. LLVM conservatively assumes two pointers may alias, which can limit vectorization across iterations. Add <code>@simd ivdep</code> to assert no loop-carried dependencies.</p><p><strong>Fast-math ops</strong> (<code>fastmath_ops</code>): FP arithmetic carrying LLVM fast-math flags (<code>contract</code>, <code>reassoc</code>, <code>nnan</code>, <code>ninf</code>, <code>nsz</code>, <code>arcp</code>, <code>afn</code>, <code>fast</code>) — the result of <code>@simd</code>/<code>@fastmath</code>. These permit floating-point reassociation and NaN/Inf assumptions that can change numerical results, not just codegen, so <code>kernel_report</code>&#39;s <code>show</code> prints a standing <code>⚠</code> warning whenever <code>fastmath_ops &gt; 0</code> rather than silently folding them into <code>fp_ops</code>. Verify against a non-fast-math reference if bit-for-bit reproducibility matters.</p><h2 id="Register-saturation:-register_report" tabindex="-1">Register saturation: <code>register_report</code> <a class="header-anchor" href="#Register-saturation:-register_report" aria-label="Permalink to &quot;Register saturation: \`register_report\` {#Register-saturation:-register_report}&quot;">​</a></h2><p>Even with good arithmetic intensity, a kernel can stall if it uses more vector registers than the CPU has available, forcing the compiler to spill values to the stack. This is invisible to <code>kernel_report</code> because it only reads LLVM IR — register allocation happens later. <a href="/StrictMode.jl/v0.3/api#StrictMode.register_report"><code>register_report</code></a> reads the native assembly instead:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;" class="repl-prompt repl-prompt-julia">julia</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;" class="repl-prompt repl-prompt-julia">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> register_report</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(my_kernel!, (Matrix{Float64},))</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">RegisterReport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> my_kernel!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Matrix{Float64})</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  32</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> zmm registers, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">53</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> spill</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(s)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  →</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> register</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">saturated</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> all </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> zmm </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> use. Adding more ILP will spill and may regress.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    This is the LLVM portable</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">compiler ceiling (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">~</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">85</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">–</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">87</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">%</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> of hand</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">asm)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">.</span></span></code></pre></div><p>A saturated reading is not a failure — it means the compiler has done everything it can. Any attempt to add more work (wider tiles, more accumulators) will only create more spills. The practical ceiling for pure <code>SIMD.jl</code> / LLVM on AVX-512 is roughly 85–87% of hand-written assembly; reaching the last ~15% requires hand-written <code>.S</code> code.</p><p>A clean reading shows room to grow:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">RegisterReport</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> my_kernel!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Matrix{Float64})</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  12</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">32</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> zmm registers, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> spill</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(s)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  →</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> clean</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 20</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> zmm free — room </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> more ILP or a wider tile.</span></span></code></pre></div><p><code>register_report</code> is meaningful only for x86-64 AVX-512 kernels; other targets return zeros.</p><h2 id="What-the-diagnostics-can&#39;t-see" tabindex="-1">What the diagnostics can&#39;t see <a class="header-anchor" href="#What-the-diagnostics-can&#39;t-see" aria-label="Permalink to &quot;What the diagnostics can&#39;t see {#What-the-diagnostics-can&#39;t-see}&quot;">​</a></h2><p>Some performance problems are invisible to static IR analysis:</p><p><strong>Algorithmic choices above the kernel</strong>: two kernel versions can look identical in IR — same vectorization, same intensity — yet differ by 25% because one packs a panel operand into contiguous memory before the inner loop. That kind of memory-orchestration choice is above the per-kernel view.</p><p><strong>Input-distribution variance</strong>: a search or formatting kernel may run 3× differently depending on the input values — same code path, different data. Benchmark across the value classes your kernel will actually see (small, large, uniform, worst-case) and report the spread rather than a single median.</p><p><strong>Data-dependent load addresses</strong>: a kernel that computes a slot index from a SIMD reduction and then loads <code>array[index]</code> exposes the full cache-miss latency of that load — the load can&#39;t issue until the reduction finishes. The guarantees are all green, but effective throughput may be limited.</p><p>The division of labor: the asserts defend the <strong>floor</strong> (vectorized, allocation-free, type-stable) — properties whose loss costs 2–100× silently. The diagnostics (<code>kernel_report</code>, <code>register_report</code>) illuminate the <strong>ceiling</strong> — they help you see how far you are from the hardware limit, and why. Neither closes every gap; some require roofline reasoning or restructuring the algorithm above the single-kernel view.</p><h2 id="Static-binary-compatibility-@assert_trim_safe" tabindex="-1">Static-binary compatibility (<code>@assert_trim_safe</code>) <a class="header-anchor" href="#Static-binary-compatibility-@assert_trim_safe" aria-label="Permalink to &quot;Static-binary compatibility (\`@assert_trim_safe\`) {#Static-binary-compatibility-@assert_trim_safe}&quot;">​</a></h2><p>Julia&#39;s ahead-of-time compiler (<code>juliac --trim=safe</code>) rejects dynamic dispatch and runtime reflection. StrictMode can flag incompatible patterns before a slow <code>juliac</code> build reveals them:</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">@assert_trim_safe</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> my_kernel</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(a, b)   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># fails if the compiled body would be rejected by --trim</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">audit</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(MyPkg; sweep </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, guarantees </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:typestable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:noalloc</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">:trimsafe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">))</span></span></code></pre></div><p>When a real <code>juliac --trim</code> run fails, <a href="/StrictMode.jl/v0.3/api#StrictMode.explain_trim"><code>explain_trim</code></a> translates the verifier output into source-mapped hints with per-site suggestions.</p>`,41)])])}const g=i(n,[["render",r]]);export{c as __pageData,g as default};
